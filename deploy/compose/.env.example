# Copy to deploy/compose/.env and replace every value before running compose.
# Do not commit real secrets.

TARU_ADMIN_TOKEN=replace-with-a-long-random-token
TARU_POSTGRES_PASSWORD=replace-with-a-long-random-password
TARU_MEDIA_ROOT=/absolute/path/to/your/movies
