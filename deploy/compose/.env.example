# Copy to deploy/compose/.env and replace every value before running compose.
# Do not commit real secrets.

NAKO_ADMIN_TOKEN=replace-with-a-long-random-token
NAKO_POSTGRES_PASSWORD=replace-with-a-long-random-password
NAKO_MEDIA_ROOT=/absolute/path/to/your/movies
