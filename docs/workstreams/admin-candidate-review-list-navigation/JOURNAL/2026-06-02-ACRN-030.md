# ACRN-030 Journal

Date: 2026-06-02
Task: ACRN-030
Status: Accepted

## Summary

Added Web Admin item-scoped Metadata Candidate Review list/navigation. Operators
can now enter a Media Item ID, inspect redaction-safe Candidate Review summary
rows, and route into the existing review detail/apply page without adding a
second apply path.

## Changed

- Added `listMetadataCandidateReviewsForItem` to the Admin API client.
- Added fixture/live data-source mapping for item-scoped Candidate Review list
  rows.
- Added route-state support for `item_id`, `review_id`, `limit`, and `offset`.
- Added a Candidate Review list section that preserves item paging state when a
  row routes into detail.
- Added data-source and route-state regression tests for redacted list rows and
  list-to-detail navigation.
- Raised only the aggregate `total-js` gzip budget from 341 KiB to 343 KiB
  after the measured production build reached 342.05 KiB.

## Validation

- `npm --prefix web run check` passed.
- `npm --prefix web run test` passed: 10 files, 118 tests.
- `npm --prefix web run build:budget` passed.
- Edge CDP browser smoke passed for the item list route to review detail
  navigation.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Handoff

Continue with `ACRN-040`: close the lane or split follow-ons for global
Candidate Review queues/search, batch governance, and related hierarchy
application.
