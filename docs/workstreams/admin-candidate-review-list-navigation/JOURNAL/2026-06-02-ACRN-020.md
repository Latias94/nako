# ACRN-020 Journal

Date: 2026-06-02
Task: ACRN-020
Status: Accepted

## Summary

Added the read-only Admin API item-scoped Metadata Candidate Review list route.
The route returns redaction-safe row summaries for navigation/triage and keeps
the existing detail/apply route as the only apply surface.

## Changed

- Added `AdminMetadataCandidateReviewListResponse` and
  `AdminMetadataCandidateReviewListEntry`.
- Added `GET /admin/v1/metadata/items/{item_id}/candidate-reviews`.
- Reused the durable repository list seam and builds an application plan per row.
- Synchronized Admin TypeScript contract source and generated outputs.
- Added a system test for item scoping, pagination, no writes, sort order, and
  response redaction.

## Validation

- `cargo nextest` failed because `cargo-nextest` is not installed in this
  environment.
- `cargo test -p nako-server admin_v1_metadata_candidate_review_list_is_item_scoped_redacted_and_read_only -- --nocapture`
  passed.
- `cargo test -p nako-api admin_contract -- --nocapture` passed.
- `cargo test -p nako-server candidate_review -- --nocapture` passed.
- `cargo fmt --all -- --check` passed.
- JSON/JSONL validation passed.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Handoff

Continue with `ACRN-030`: Web Admin item-scoped Candidate Review list/navigation
that consumes the new Admin route and routes into the existing detail/apply page.
