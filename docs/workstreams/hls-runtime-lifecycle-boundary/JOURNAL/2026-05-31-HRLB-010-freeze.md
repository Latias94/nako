# HRLB-010 - Lifecycle Freeze

Date: 2026-05-31
Status: Done with concerns

## Work Completed

- Read the HRLB workstream docs, ADR 0052, playback architecture map, lane
  routing, HLS progressive runtime handoff, seek/restart handoff, playback
  resource scheduler handoff, and storage health closeout.
- Froze current HLS lifecycle invariants in `DESIGN.md` without changing Rust
  behavior.
- Added cleanup ownership and test coverage maps for same-generation active
  requests, finished reuse, generation supersede, running playlist readiness,
  segment readiness and one-shot wait, cancellation/timeout cleanup, startup
  stale recovery, terminal artifact cleanup, staging input release, and artifact
  I/O pressure.
- Recorded the PAIP decision: artifact I/O pressure should split to the
  existing `proposed:hls-artifact-io-pressure-enforcement` follow-on rather
  than entering `HRLB-020`.

## Coverage Concerns

- HLS timeout cleanup is implemented in the runner branch but lacks a focused
  HLS timeout cleanup regression test.
- Startup stale-session recovery is covered generically for transcode sessions,
  but not with an HLS fixture.
- Staging lease acquire/release primitives are covered, but HLS remote staged
  input release is not directly covered across success, error, and admission
  rejection paths.

## Validation

```text
python -m json.tool docs/workstreams/hls-runtime-lifecycle-boundary/WORKSTREAM.json
git diff --check -- docs/workstreams/hls-runtime-lifecycle-boundary docs/architecture/PLAYBACK.md docs/architecture/LANES.md docs/architecture/WORKSTREAM_LINKS.md docs/workstreams/README.md
```

Both commands passed on 2026-05-31. The diff check emitted only Git
line-ending normalization warnings for touched docs/JSON files and no
whitespace errors.

## Next Action

Return the freeze to planner review. After acceptance, `HRLB-020` should add
focused behavior-preserving tests and only then extract a coordinator/facade if
it reduces scattered lifecycle ownership.
