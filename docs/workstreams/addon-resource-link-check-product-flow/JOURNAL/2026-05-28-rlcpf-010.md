# RLCPF-010 Journal

Opened `addon-resource-link-check-product-flow` after the protocol/client
contract lane closed.

Decision:

- Route consumes opaque resource-search selection ids.
- Body contains `refresh` only.
- Host-owned session supplies the raw selected link for addon protocol traffic.
- Product response returns only safe facts.
