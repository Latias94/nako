# 2026-05-20

- Opened workstream after closing Android end-to-end validation hardening.
- Reviewed `docs/api/HTTP_API.md`, the closed Android public API coverage
  matrix, `ClientBrowseDataSource`, and `BrowseSession`.
- Current answer: Android has already connected the core server-backed Public
  Client API for setup, browse, details, search, artwork, source facts,
  playback, playback session lifecycle, Continue Watching, and watched/progress
  reporting.
- Remaining high-value product gap: Person Detail and relationship browsing
  indexes, not the core playback/detail API.
- APICI-010 completed by creating the current matrix and task ledger.
- APICI-020 completed:
  - added `PersonResponse`;
  - added `TaruBrowseClient.personDetail`;
  - added `MissingPerson` diagnostics and UI failure title;
  - added focused tests for request building, decoding, 404 diagnostics,
    unsupported API version, and blank person ID local failure.
- Focused `TaruBrowseClientTest` passed.
- APICI-030 completed:
  - added `TaruRoute.PersonDetail`;
  - added `PersonDetailUiState`;
  - added `BrowseAction.OpenPersonDetail`;
  - added `BrowseDataSource.loadPersonDetail`;
  - added `ClientBrowseDataSource.loadPersonDetail` that calls
    `personDetail` and then `listPersonItems`;
  - added navigation save/restore for Person Detail;
  - added a temporary `TaruBrowseShell` bridge that renders Person Detail
    related items through existing facet results until APICI-040 creates the
    dedicated screen.
- Focused UI browse tests and full debug unit gate passed.
- APICI-040 completed:
  - added a dedicated Person Detail route screen under
    `ui/screens/person/`;
  - removed the temporary `PersonDetailUiState.toFacetState()` rendering
    bridge from `TaruBrowseShell`;
  - changed Cast & Crew presentation so stable `person_id` rows dispatch
    Person Detail navigation;
  - kept missing-ID credits as explicit Person facet/API-gap fallbacks;
  - added focused detail presentation tests for the relationship target
    contract.
- Focused detail presentation test and full debug unit gate passed.
- Next task is APICI-050: prove the server-backed Person Detail path through
  focused `profile-with-media` smoke.
