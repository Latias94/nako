# WMLP-030A Public Media Read Model Boundary

Date: 2026-05-28
Status: DONE

## Summary

Added Public Media read-model boundaries without wiring route UI yet.

- List payloads now carry page and readiness states.
- Detail payloads carry sources and image refs for future playback entry.
- Library payloads expose library metadata and source readiness.
- Library-scoped item browse remains an explicit missing contract instead of a
  fake live result.

The existing playback plan draft in `media-data-source.ts` remains intact and
validated, but playback wiring remains deferred to WMLP-040.

## Validation

- `npm --prefix web run test -- src/test/data-source-contracts.test.ts`
  - Passed: 1 file, 16 tests.
- `npm --prefix web run check`
  - Passed: `tsc --noEmit`.
- `npm --prefix web run build:budget`
  - Passed: media route JS 56.81 KiB raw / 16.11 KiB gzip; total JS
    1048.30 KiB raw / 309.49 KiB gzip.
