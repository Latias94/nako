# WMLP-050 Playback State

Date: 2026-05-28
Status: DONE

## Summary

Wired user playback-state contracts into the Public Media data source.

- `listContinueWatching` maps item/state/image DTOs.
- `getPlaybackState` maps item state.
- `updatePlaybackProgress` writes progress through Public Client.
- `setWatchedState` writes watched/unwatched through Public Client.
- Home continue-watching now uses playback-state data with fixture fallback.

Browser heartbeat remains separate from playback-state writes until the browser
playback session-id contract exists.

## Validation

- `npm --prefix web run test`
  - Passed: 8 files, 49 tests.
- `npm --prefix web run check`
  - Passed: `tsc --noEmit`.
- `npm --prefix web run build:budget`
  - Passed: media route JS 42.76 KiB raw / 11.75 KiB gzip; total JS
    1053.15 KiB raw / 311.69 KiB gzip.
