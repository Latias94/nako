# WMLP-030 Live Browse And Detail Parity

Date: 2026-05-28
Status: DONE

## Summary

Completed the live read-model route slice.

- `/media` continues to use the Public Media list seam and carries readiness
  for missing Recently Added sort/filter contract.
- `/media/detail` now receives Public Media detail payloads with live item,
  source, image, fallback, and readiness state.
- `/media/search` now searches through the Public Media data source instead of
  scanning only local mock results.
- `/media/library` reads library metadata/source readiness and shows the
  missing library-scoped item browse contract instead of faking scoped live
  items.

## Validation

- `npm --prefix web run test`
  - Passed: 8 files, 47 tests.
- `npm --prefix web run check`
  - Passed: `tsc --noEmit`.
- `npm --prefix web run build:budget`
  - Passed: media route JS 42.45 KiB raw / 11.62 KiB gzip; total JS
    1051.05 KiB raw / 310.97 KiB gzip.
