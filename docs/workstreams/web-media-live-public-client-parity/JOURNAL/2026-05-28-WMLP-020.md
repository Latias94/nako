# WMLP-020 Public Client Readiness Audit

Date: 2026-05-28
Status: DONE

## Summary

Audited the committed generated TypeScript SDK, committed Public Client route
inventory, and current `web/src/api/public/media-data-source.ts` seam for Media
Web live parity.

Ready contracts:

- `listItems`, `searchItems`, and `getItem` cover the first home/search/detail
  live read slice.
- `listItemCredits`, `listItemImages`, and `managementContextLinks` can support
  richer detail payloads.
- Playback decision, browser ticket, playback session, HLS segment, and
  playback-state methods exist for later WMLP-040/WMLP-050 tasks.

Gap:

- Library metadata/source contracts exist, but library-scoped item browse does
  not. WMLP-030 must not present mock library item browse as a live scoped
  result.

## Validation

- `npm --prefix web run test -- src/test/data-source-contracts.test.ts`
  - Passed: 1 file, 13 tests.
- `npm --prefix web run check`
  - Passed: `tsc --noEmit`.
- `python -m json.tool docs/workstreams/web-media-live-public-client-parity/WORKSTREAM.json`
  - Passed.
- `git diff --check -- docs/workstreams/web-media-live-public-client-parity`
  - Passed.
