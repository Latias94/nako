# FFR-040F API Admin Catalog Governance Surface Split

Date: 2026-05-23
Status: DONE

## Scope

- `crates/nako-api/src/admin.rs`
- `crates/nako-api/src/admin/catalog_governance.rs`
- `docs/workstreams/fearless-future-architecture-refactor/*`

## Changes

- Added `admin/catalog_governance.rs` and moved catalog governance DTOs out
  of `admin.rs`.
- The catalog governance module now owns conversion from
  `CatalogGovernanceItemRecord` into admin-safe governance summaries.
- Kept **Local Inference Evidence** summarized as `has_evidence`,
  confidence, source, and version without exposing raw `evidence_value`.
- Kept the public `nako_api::admin::*` import shape stable through
  `pub use catalog_governance::*`.
- Moved the catalog governance redaction test into the new module.

## Validation

- `cargo check -p nako-api --tests` passed.
- `cargo nextest run -p nako-api catalog_governance --no-fail-fast` passed
  with 1 test run and 54 skipped.
- `cargo nextest run -p nako-api admin_contract --no-fail-fast` passed with 5
  tests run and 50 skipped.
- `cargo nextest run -p nako-api public_client --no-fail-fast` passed with 9
  tests run and 46 skipped.
- `cargo nextest run -p nako-api --no-fail-fast` passed with 55 tests run.
- `cargo fmt --package nako-api` completed.
- `cargo fmt --package nako-api -- --check` passed.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Judgment

- FFR-040 has now split the admin API surfaces with meaningful behavior and
  redaction locality: managed artwork, playback, network, storage,
  automation, intake, operations, and catalog governance.
- The remaining `admin.rs` width is server config diagnostics and overview
  summaries. They are broad but mostly passive DTO aggregates, so another
  split would mostly shorten the file rather than deepen a module.
- FFR-040 is ready for review and handoff to FFR-050.
