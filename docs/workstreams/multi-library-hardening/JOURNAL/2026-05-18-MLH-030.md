# MLH-030 Journal

Date: 2026-05-18

## Summary

Implemented a named startup Library reconciliation boundary for Taru's
multi-library hardening lane.

## What Changed

- Added `ConfiguredLibraryReconciliationService` as a focused startup
  boundary in `crates/taru-server/src/app/library_reconciliation.rs`.
- Extended `ServerStartupReport` to carry a reconciliation report with counts
  for configured, added, updated, unchanged, and retained unconfigured
  libraries.
- Added characterization coverage for the new reconciliation report in
  `crates/taru-server/src/app/tests/startup.rs`.

## Validation

- `cargo check -p taru-server --tests`
- `cargo check -p taru-db --tests`
- `cargo nextest run -p taru-server startup --no-fail-fast`
- `cargo nextest run -p taru-server app_startup_reports_configured_library_reconciliation --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`

## Follow-Up

MLH-040 should narrow remaining config-driven library lookups where database
Library authority is now available after startup.
