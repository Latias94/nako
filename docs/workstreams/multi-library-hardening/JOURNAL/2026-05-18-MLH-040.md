# MLH-040 Journal

Date: 2026-05-18
Task: MLH-040
Status: DONE

## Summary

MLH-040 narrowed workflow callers so Library fields come from the reconciled
database row after startup. Configuration remains the desired-state input and
still owns backend credentials plus host-local storage roots.

## Implementation Notes

- Scan job execution now loads `Library` through `LibraryRepository` and uses
  persisted scan options when constructing the VFS scanner.
- Metadata maintenance and refresh now resolve Library profile information from
  the media source's persisted Library ID.
- NFO import no longer stores a full `NakoServerConfig`; it resolves local
  metadata policy from the persisted Library.
- Storage diagnostics now enumerates persisted libraries and reports retained
  unconfigured libraries as unavailable, while still using config to resolve
  backend credentials and local physical roots.

## Verification

- `cargo nextest run -p nako-server storage --no-fail-fast`
- `cargo check -p nako-server --tests`
- `cargo check -p nako-db --tests`
- `cargo nextest run -p nako-server startup --no-fail-fast`
- `cargo nextest run -p nako-server metadata --no-fail-fast`
- `cargo nextest run -p nako-server nfo --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`
- `cargo nextest run -p nako-server --no-fail-fast`

All commands passed in the current workspace.

## Follow-Up

Run MLH-050 closeout review. If new behavior is needed for retained
unconfigured libraries, Library Access, or admin mutation APIs, split that work
into separate lanes instead of expanding this hardening lane.
