# NRCT-060 Journal

Date: 2026-05-27

## Work Completed

- Updated Admin renderer readiness to report Nako remote-client cast-safe
  transport as ready.
- Kept Chromecast, DLNA, and AirPlay protocol adapters as planned follow-ons.
- Regenerated the Admin Web TypeScript contract.
- Accepted ADR 0041 and removed stale planned-transport wording from ADR 0040.

## Validation

```powershell
cargo nextest run -p nako-server admin_v1_playback_renderers_reports_safe_diagnostics_and_adapter_readiness --no-fail-fast
cargo nextest run -p nako-api -E 'test(admin_contract) | test(public_openapi)' --no-fail-fast
cargo nextest run -p nako-server -E 'test(renderer) | test(admin)' --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

Results: all commands passed. `git diff --check` printed CRLF conversion
warnings only.

## Next

Run `NRCT-070`, then open the protocol casting workstream.
