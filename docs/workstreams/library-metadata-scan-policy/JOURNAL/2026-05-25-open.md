# 2026-05-25 - Open LMSP

Opened `library-metadata-scan-policy` after real local and NAS smoke showed
that scan/probe/playback works, but local NFO metadata requires a separate
manual `import-nfo` command.

The first slice is deliberately narrow:

- add a profile-derived scan-time metadata acquisition plan;
- wire scan to run NFO Import when the plan enables it;
- verify against `H:\Super\Videos` and a NAS single-directory SMB smoke.

Provider refresh, Addon Bulk Metadata Scrape, embedded readers, image/artwork
discovery, and full NAS scan progress are split follow-ons.
