# 2026-05-25 - LMSP-020

Implemented the first metadata scan policy model.

- Added `MetadataScanPolicy` to `MetadataProfile`.
- Added `MetadataProfile::scan_acquisition_plan`.
- The current executable plan only enables local NFO import.
- Future provider refresh, Addon scrape, embedded read, sidecar read, and image
  discovery remain explicit false plan slots.
- Added server config support for per-library metadata profiles via
  `metadata.library_profiles`.

Validation:

- `cargo nextest run -p nako-core metadata_profile_builds_scan_acquisition_plan --no-fail-fast`
- `cargo nextest run -p nako-server config_applies_library_metadata_profile_overrides --no-fail-fast`
