# FAD-070 — Search Semantics

Date: 2026-05-20

## Scope

Added measured search semantics and projection-version discipline before AI or
vector search broadens Nako search behavior.

## Changes

- Added a shared `nako-search` evaluation harness for title, alias, body, facet,
  exact Browse Facet, pagination, and CJK-friendly compact matching semantics.
- Added projection-version freshness helpers on `SearchDocument` and
  `SearchEvaluationDocument`.
- Refactored SQLite and PostgreSQL `SearchIndex` adapters to load rows and
  delegate semantics to `nako-search`, removing duplicated backend scoring.
- Updated Catalog hydration to project accepted Provider Subject titles/keys
  into Search Projection aliases/body/facets.
- Widened `NfoImportRepository` to include `ProviderMappingRepository` because
  NFO catalog projection planning now uses Provider Mapping context.

## Validation

```bash
cargo fmt --all
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo check -p nako-search -p nako-catalog -p nako-db --tests
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo check -p nako-nfo -p nako-metadata -p nako-server --tests
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo nextest run -p nako-search --no-fail-fast
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo nextest run -p nako-catalog semantic_search --no-fail-fast
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo nextest run -p nako-db search --no-fail-fast
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo nextest run -p nako-db facet --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

Results:

- `cargo check -p nako-search -p nako-catalog -p nako-db --tests` passed.
- `cargo check -p nako-nfo -p nako-metadata -p nako-server --tests` passed.
- `cargo nextest run -p nako-search --no-fail-fast` passed: 6 passed.
- `cargo nextest run -p nako-catalog semantic_search --no-fail-fast` passed:
  1 passed, 3 skipped.
- `cargo nextest run -p nako-db search --no-fail-fast` passed: 7 passed,
  97 skipped.
- `cargo nextest run -p nako-db facet --no-fail-fast` passed: 1 passed,
  103 skipped.
- `cargo fmt --all -- --check` passed.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Environment Note

The default user temp drive had no free space and caused linker `LNK1108`
failures before final validation was retried with `TMP`/`TEMP` set to
`F:\Temp`.

## Handoff

Continue with FAD-080 test-locality cleanup. Keep the cleanup evidence-driven:
extract fixtures around touched Interfaces only where it improves reviewability
without weakening behavior coverage.
