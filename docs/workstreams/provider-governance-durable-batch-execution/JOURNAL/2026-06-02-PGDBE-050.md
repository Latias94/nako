# PGDBE-050 Journal

Date: 2026-06-02

## Scope

Implemented the Web Admin durable Candidate Review batch status slice.

## Changes

- Added Web Admin API client methods for durable Candidate Review batch create
  and status routes.
- Added Web read models and fixture/live data-source support for durable batch
  status, selection, execution summary, and redaction-safe item outcomes.
- Changed Candidate Review batch confirmation to create a durable batch from
  selected live plan rows.
- Rendered compact durable batch status in the Candidate Review Admin page and
  polled queued/running batches through TanStack Query.
- Removed the obsolete Web synchronous Candidate Review `batch-apply` client
  method and read-model mapper.
- Added data-source and route-state coverage for durable batch creation,
  status lookup, UI status rendering, and raw secret/key redaction.

## Validation

- `npm --prefix web run check`
  - Result: passed.
- `npm --prefix web run test`
  - Result: passed, 10 test files / 122 tests.
- `npm --prefix web run build:budget`
  - Result: passed, total JS gzip 344.96 KiB against the 345 KiB limit.
- `git diff --check`
  - Result: passed; Git emitted CRLF normalization warnings only.
- Local HTTP route smoke:
  - `http://127.0.0.1:3000/admin/metadata/candidate-reviews?status=accepted&provider=bangumi&limit=25&offset=0`
    returned HTTP 200 with 1155 bytes.
  - The same route on port `3001` returned HTTP 200 with 1155 bytes.

## Notes

- Browser plugin tooling was not available in this session. A new Vite server
  attempt on port `3002` listened but did not respond to HTTP requests, so it
  was stopped. Existing local Web servers on ports `3000` and `3001` completed
  the route smoke.
- Public Client API exposure, audit/undo, provider endpoint breadth, and related
  hierarchy application remain split from this workstream.
