# PGDBE-010 - Opening Journal

Date: 2026-06-02
Status: DONE

Opened `provider-governance-durable-batch-execution` after Provider Governance
Bulk Review closeout.

Decision:

- make durable Candidate Review batch execution the next active workstream;
- start with core/DB durable batch state and repository contract;
- use ADR 0053 durable job/runtime boundaries for execution later;
- keep related hierarchy application, Public Client API exposure, provider
  endpoint depth, and audit/undo governance split.

Evidence:

- PGBR closeout explicitly split durable execution as a follow-on.
- Existing synchronous batch apply is bounded and safe but not persisted.
- Generated Artifact bulk apply provides a useful durable batch/job precedent,
  but Candidate Review must own its own state.
- A read-only explorer independently recommended this lane and the same
  initial task shape.

Next:

- `PGDBE-020`: add Candidate Review durable batch core/DB model and repository
  contract tests.
