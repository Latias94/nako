# 2026-05-21 — MIS-020 Durable Import Artifact Domain

## Scope

Implemented the durable Managed Import artifact domain/schema slice in
`taru-core` and `taru-db`.

## TDD Notes

- Added a backend-neutral `managed_import` contract first.
- The first run failed at compile time because no repository implementation
  existed for SQLite, PostgreSQL, or the facade.
- Implemented the core domain, repository trait, migrations, adapters, and
  facade forwarding to make the contract pass.

## Evidence

- `cargo nextest run -p taru-db managed_import --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`

## Follow-Up

MIS-030 should expose server app diagnostics on top of this durable repository
without external fetches or library-root writes.
