# 2026-05-21 — LNA-020 VFS Link Dry-Run

Implemented the first non-destructive link authority primitive:

- `StorageLinkKind`
- `StorageLinkPlanRequest`
- `StorageLinkPlanStatus`
- `StorageLinkPlan`
- default `StorageBackend::plan_link` unsupported response
- `LocalFsBackend` dry-run diagnostics for ready, existing target, missing
  source, missing parent, non-file source, and security violations

One targeted gate failed during development: missing target parent initially
escaped as a low-level storage IO error. The fix keeps planning non-destructive
by resolving the target parent only after checking whether it exists.

Actual symlink/hardlink mutation remains out of scope.
