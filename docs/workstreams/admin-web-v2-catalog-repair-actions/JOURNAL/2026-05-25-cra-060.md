# CRA-060 Verification And Browser Smoke

Date: 2026-05-25

## Summary

Ran focused verification and browser smoke for the Catalog Governance repair
action slice.

## Commands

- `cd apps/admin-web && npm run check`
- `cd apps/admin-web && npm run test -- App.test.tsx adminApi/client.test.ts adminApi/dataSource.test.ts`
- `cargo nextest run -p nako-server admin_v1_catalog_governance_provider_mapping_review_mutates_idempotently --no-fail-fast`
- `cargo test -p nako-api admin_contract_includes_route_constants -- --nocapture`
- `cargo fmt --all --check`
- `git diff --check`
- `cd apps/admin-web && npm run test`
- `cd apps/admin-web && npm run build`
- `cargo nextest run -p nako-server catalog_governance --no-fail-fast`

## Browser Smoke

The local Admin Web server was started at `http://127.0.0.1:5177/`.

Browser smoke used `playwright-cli` because the Browser plugin's Node REPL
execution tool was not exposed in this session.

Checks:

- desktop queue at `1440x1000`;
- repair context at desktop size;
- explicit prepare/confirm success path;
- visible failure path using a non-JSON mutation response;
- mobile repair context at `390x844`;
- document-level horizontal overflow checks;
- console error checks;
- unsafe text exclusion checks for Source Locator, local path, raw evidence,
  provider raw body/URL, NFO XML/path, token, credential, and private path text.

## Result

Pass. Continue with CRA-070 closeout.
