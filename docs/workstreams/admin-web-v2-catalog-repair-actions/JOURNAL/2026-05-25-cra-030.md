# CRA-030 Repair Detail And Review Plan

Date: 2026-05-25

## Claim

Catalog Governance now has redaction-safe Admin API detail and Provider Mapping
review-plan routes for the accepted first repair action.

## Changes

- Added Admin DTOs for:
  - Catalog Governance item detail;
  - Provider Mapping summary;
  - Provider Subject safe summary;
  - Provider Mapping review-plan request/response;
  - repair readiness, reasons, and boundary booleans.
- Added `CatalogAppService` methods for:
  - one-item detail assembly;
  - Provider Mapping review-plan assembly.
- Added Admin routes:
  - `GET /admin/v1/catalog/governance/items/{item_id}`;
  - `POST /admin/v1/catalog/governance/items/{item_id}/provider-mappings/{mapping_id}/review-plan`.
- Regenerated Admin Web generated contract and added `AdminApiClient` wrappers.
- Updated `docs/api/HTTP_API.md` with the implemented routes and redaction
  contract.

## Validation

- `cargo nextest run -p nako-server admin_v1_catalog_governance_provider_mapping_review_plan_is_redacted --no-fail-fast`
- `cargo test -p nako-api admin_contract_includes_route_constants -- --nocapture`
- `cd apps/admin-web && npm run generate:admin-api`
- `cd apps/admin-web && npm run check`
- `cd apps/admin-web && npm run test -- adminApi/client.test.ts`
- `cargo fmt --all --check`
- `git diff --check`

All passed. Git reported existing LF-to-CRLF working-copy warnings during
`git diff --check`; no whitespace errors were reported.

## Handoff

Continue with CRA-040. Add the confirmed Provider Mapping review mutation,
idempotent result semantics, no-fake-fallback Admin Web wrappers, and focused
mutation tests before adding UI controls.
