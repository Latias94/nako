# ACF-030C Compose UI Baseline Rewrite

Status: DONE_WITH_CONCERNS
Date: 2026-05-18

## Scope

Replaced the tracer-oriented Android Compose browse/detail shell with the first
implementation UI baseline while preserving the existing connection, token,
profile, browse client, DTO, diagnostics, and unit-test foundations.

## Changed

- Added Compose Material Icons Extended.
- Passed the real server profile snapshot into the browse shell and persisted
  profile switches from Server Profile.
- Split the browse UI into:
  - `NakoBrowseShell.kt`
  - `BrowseModels.kt`
  - `BrowseComponents.kt`
  - `BrowseFormatters.kt`
  - `HomeScreen.kt`
  - `LibrariesScreen.kt`
  - `MediaItemDetailScreen.kt`
  - `SettingsScreens.kt`
  - `PlaceholderScreens.kt`
  - `NakoBrowseShellPreview.kt`
- Rebuilt Home, Libraries, Media Item Detail, Settings Home, and Server
  Profile around the documented v2 Material 3 baseline.
- Kept Search and Browse Facet Result as explicit navigable placeholders.

## Validation

- `apps/android/gradlew.bat -p apps/android :app:testDebugUnitTest` passed.
- `apps/android/gradlew.bat -p apps/android :app:assembleDebug` passed.
- `git diff --check` passed with line-ending normalization warnings only.

## Concern

No manual debug walkthrough was run against a live Nako server fixture or
Android device/emulator in this session. Track that under `ACF-030D` before
closing the full ACF-030 browse loop.
