# DWI-060 Closeout And Follow-On Split

Date: 2026-05-22
Task: DWI-060
Status: DONE

## Summary

Closed `downloads-watch-folder-intake` after fresh verification. The lane now
has durable intake candidates, app-service Managed Import handoff,
storage/VFS-backed watch-folder discovery, and Admin-only redacted diagnostics.
The closeout returned the next lane decision to `post-rpd-product-hardening`.

## Review Result

No blocking findings.

The shipped target state is met:

- backend-neutral acquisition intake candidate persistence exists;
- watch-folder discovery is idempotent and uses storage/VFS list/stat seams;
- intake acceptance creates or links Managed Import artifacts only;
- Admin diagnostics are redacted and Admin-only;
- Public Client API and `nako-client-protocol` remain unchanged.

## Split Follow-Ons

The following were deliberately not hidden inside this lane:

- torrent, Usenet, RSS, browser, or external download-client protocol adapters;
- background watch-folder scheduling or OS watcher runtime;
- Admin web intake workflow polish beyond typed diagnostics;
- automatic promotion apply;
- NFO sidecar mutation shortcuts;
- remote access/tunnel runtime;
- AI-generated artifact proposal queues;
- Addon runtime/distribution and side-effect permission UX.

## Fresh Verification

- `cargo nextest run -p nako-db acquisition_intake --no-fail-fast` — pass, 1
  passed, 123 skipped.
- `cargo nextest run -p nako-server acquisition_intake --no-fail-fast` — pass,
  6 passed, 229 skipped.
- `cargo nextest run -p nako-api admin_contract --no-fail-fast` — pass, 5
  passed, 45 skipped.
- `cargo nextest run -p nako-server http::tests::system --no-fail-fast` — pass,
  19 passed, 216 skipped.
- `npm run check` from `apps/admin-web` — pass.
- `cargo fmt --all -- --check` — pass.
- `python -m json.tool docs/workstreams/downloads-watch-folder-intake/WORKSTREAM.json`
  — pass.
- `python -m json.tool docs/workstreams/post-rpd-product-hardening/WORKSTREAM.json`
  — pass.
- `git diff --check` — pass with repository CRLF conversion warning for the
  unrelated `sdk/kotlin` working-tree change only.
- `git diff --name-only -- crates/nako-client-protocol` — no output.

## Next

Return to `post-rpd-product-hardening`. Recommended next mainline lane:
`network-access-boundary`, scoped first to endpoint/proxy/tunnel policy and
readiness rather than built-in NAT traversal runtime.