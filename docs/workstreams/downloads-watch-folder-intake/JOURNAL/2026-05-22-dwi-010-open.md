# DWI-010 — Lane Open

Date: 2026-05-22

## Summary

Opened `downloads-watch-folder-intake` as the next mainline child of
`post-rpd-product-hardening` after Playback/Transcode Ops Hardening closeout.

The lane is acquisition-intake focused. It should discover or accept candidates,
record redacted diagnostics, and hand accepted candidates into Managed Import
artifacts and existing promotion/apply workflows. It must not become a generic
built-in downloader or bypass library file-write authority.

## Boundary Decisions

- Watch folders are candidate sources, not trusted library roots.
- Intake candidates are not Media Sources.
- Intake acceptance creates or links Managed Import artifacts; promotion apply
  and NFO sidecar apply remain separate accepted workflows.
- VFS/storage list/stat primitives should own watch-folder path safety.
- Admin diagnostics are in scope; Public Client API changes are not.
- Protocol-specific downloader clients, network traversal, AI writes, Addon
  runtime, Admin UI polish, background scheduling, and automatic apply behavior
  are follow-ons unless explicitly opened.

## Verification

- `python -m json.tool docs/workstreams/downloads-watch-folder-intake/WORKSTREAM.json`
  — pass.
- `python -m json.tool docs/workstreams/post-rpd-product-hardening/WORKSTREAM.json`
  — pass.
- `git diff --check` — pass with repository CRLF conversion warnings only.

## Next

Execute DWI-020: durable acquisition intake candidate domain/persistence and
backend-neutral contract tests.