# AMG-030

## Summary

Added the first operator-confirmed Addon Manager lifecycle plan slot.

## Changes

- Added `POST /admin/v1/addons/{addon_id}/manager-plan`.
- Added `AdminAddonLifecycleIntent` plus manager-plan request/response DTOs.
- Kept `GET /admin/v1/addons/{addon_id}/manager-plan` as the redaction-safe snapshot.
- Extended the official addon smoke script to confirm the manager plan against a current-source Nako image.

## Validation

- `cargo fmt --all -- --check`
- `cargo nextest run -p nako-server addon --no-fail-fast`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/official-addon-e2e-smoke.ps1 -NakoImage nako:amg030`

## Notes

- Operator confirmation is explicit and required for install/update/remove plan requests.
- Marketplace, package signing, provider breadth, and process/container supervision remain follow-on lanes.
