# 2026-05-20

- Opened workstream after closing player route host refactor.
- Baseline: Android QA harness, smoke regression harness, developer validation
  entrypoint, detail facet smoke, and playback depth validation are already
  closed.
- Direction: harden evidence contracts around existing smoke navigation instead
  of adding another navigation implementation.
- Added state-level `report.json` output from `Smoke-Emulator.ps1`.
- Updated `Smoke-Regression.ps1` to link each state's Markdown and JSON report
  in both Markdown and JSON regression summaries.
- Validation:
  - Script parse gate passed.
  - Controlled invalid-serial smoke regression generated a stable failure
    `report.json`.
  - Focused `empty-setup` smoke regression passed and linked state reports.
  - `Validate-AndroidLocal.ps1 -SkipSmoke` passed.
  - `git diff --check` passed with line-ending normalization warnings only.
- Closeout:
  - First full default validation exposed a stale shared demo fixture database
    with an old migration checksum under `apps/android/build/demo-fixtures/`.
  - Media fixture preparation now writes under the current smoke evidence
    directory for `profile-with-media` and `profile-active-remux`.
  - `profile-with-media` post-player detail evidence now expects `Play` after
    Direct Play completion, matching server readback where the item is watched
    and Continue Watching is cleared.
  - Cast & Crew evidence now scrolls to the actual person row before asserting
    person route text.
  - Focused `profile-with-media` smoke regression passed:
    `apps/android/build/smoke-regression/20260520-112424/report.md`.
  - Full default `Validate-AndroidLocal.ps1` passed:
    `apps/android/build/validation/20260520-112917/report.md`.
  - Workstream closed.
