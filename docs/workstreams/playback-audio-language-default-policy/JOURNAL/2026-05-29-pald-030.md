# PALD-030 Journal - HLS Audio Rendition Defaults

Date: 2026-05-29
Task: PALD-030

## Work Completed

- Added `preferred_audio_language` to the public HLS playlist query path as a
  comma-separated ordered language list.
- Mapped that query into the request-scoped playback preference context so HLS
  uses the same selected audio policy as the playback planner.
- Added route coverage for language-selected HLS audio rendition defaults,
  explicit `audio_stream` override precedence, and normalized request identity
  reuse.
- Updated HLS test fixtures so fake FFmpeg creates audio sidecar playlists and
  segments for multi-audio public route tests.
- Exposed the query in OpenAPI, TypeScript SDK, Kotlin SDK, and HTTP API docs.

## Evidence

- `cargo nextest run -p nako-server preferred_audio_language --no-fail-fast`
- `cargo nextest run -p nako-server hls --no-fail-fast`
- `cargo nextest run -p nako-server playback --no-fail-fast`
- `cargo nextest run -p nako-api --no-fail-fast`

## Follow-On

PALD-040 should run final closeout gates and decide whether to close the lane or
split follow-ons for persisted preferences, subtitle language policy,
codec-aware audio, downmix/normalization, UI controls, LL-HLS/DASH/DRM, and
offline sync.
