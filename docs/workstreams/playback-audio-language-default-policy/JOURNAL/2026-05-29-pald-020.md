# PALD-020 Journal - Request-Scoped Audio Language Preference

Date: 2026-05-29
Task: PALD-020

## Work Completed

- Added `preferred_audio_languages` to `PlaybackPreferenceContext`.
- Added probe-aware audio selection in `PlaybackTargetProfile`:
  explicit requested audio stream, preferred language match, then default
  fallback.
- Normalized preferred language identity values with a safe BCP-47-like subset.
- Moved server HLS setup to use the playback decision's transcode requirement
  track selection instead of recomputing from request facts.
- Added playback tests for language match, explicit override, fallback, and
  identity normalization.
- Added server playback coverage proving a preferred audio language selects the
  matching HLS source audio stream.

## Evidence

- `cargo nextest run -p nako-playback audio --no-fail-fast`
- `cargo nextest run -p nako-server playback --no-fail-fast`
- `cargo nextest run -p nako-server hls --no-fail-fast`

## Follow-On

PALD-030 should make the policy wire-visible through HLS request parsing and
assert generated audio rendition default flags from the public playback path.
