# RPD-040 — Release Artifact Script And CI Shape

Date: 2026-05-21
Status: DONE

## Changes

- Added `scripts/package-release.ps1` and `scripts/package-release.sh`.
- Added `docs/deployment/RELEASE_ARTIFACTS.md`.
- Added `.github/workflows/release-package.yml`.
- Packaging output includes archive, copied release manifest, and `SHA256SUMS`.
- Archive includes the server binary, release manifest, license/readme,
  Dockerfile, compose/config examples, and deployment docs.

## Validation

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package-release.ps1 -WhatIf
bash scripts/package-release.sh --dry-run
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package-release.ps1 -SkipBuild -OutputDir target/package-release-rpd040
```

Result: Pass. Bash dry-run works in the current WSL environment even when rustc
is unavailable.

## Next

Continue with RPD-050 operator release checklist and packaged install docs.
