# RPD-020 — Config Preflight

Date: 2026-05-21
Status: DONE

## Changes

- Added `taru-server config-check` with text and `--json` output.
- Added `--create-dirs` to explicitly create and write-probe Taru-owned remux
  staging and artwork artifact directories.
- Added redaction-safe config preflight checks for database backend/URL scheme,
  unresolved database templates, bind/auth shape, auth token environment
  presence, local/WebDAV media library shape, duplicate library IDs, duplicate
  library roots, and runtime directories.
- Updated self-hosted deployment docs to run `config-check` before `serve`.

## Validation

```powershell
cargo nextest run -p taru-server config --no-fail-fast
```

Result: Pass.

## Next

Continue with RPD-030 container build and compose packaging.
