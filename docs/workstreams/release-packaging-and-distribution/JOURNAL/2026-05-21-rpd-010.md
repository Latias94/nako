# RPD-010 — Open Release Packaging And Distribution

Date: 2026-05-21
Status: DONE

## Notes

- Opened after `self-hosted-release-readiness` completed.
- The release baseline already has local gates, PostgreSQL harnesses, deployment
  examples, backup/restore docs, and self-host smoke.
- This lane owns the next operator gap: artifact contract, startup/config
  preflight, container shape, release artifact scripts, checksums, and install
  docs.
- Recorded Artifact Contract V0: shipped artifacts, excluded runtime state and
  secrets, durable runtime layout, and operator safety contract.
- Inventoried current deploy/release state: config examples and release gates
  exist; Taru server Dockerfile, `.dockerignore`, full compose app stack,
  package script, artifact manifest, and checksums are still absent and owned by
  later RPD tasks.

## Next

Continue with RPD-020 config preflight.
