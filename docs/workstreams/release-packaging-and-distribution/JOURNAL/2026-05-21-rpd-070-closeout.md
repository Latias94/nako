# RPD-070 — Closeout

Date: 2026-05-21
Status: DONE

## Closeout Summary

The release packaging and distribution workstream is complete. Nako now has:

- Artifact Contract V0 and baseline inventory.
- `nako-server config-check` with redaction-safe preflight.
- `database_url_env` for secret database URL injection.
- Nako server `Dockerfile` and `.dockerignore`.
- SQLite/PostgreSQL container configs and compose stacks.
- `release-gate` container mode.
- Package scripts for PowerShell and Bash.
- Release manifest and checksum output.
- GitHub Actions release package workflow shape.
- Operator self-hosted, release artifact, release checklist, and
  backup/restore/upgrade docs.
- Future-lane decision matrix with Metadata Provider Breadth recommended next.

## Fresh Gates

```powershell
cargo fmt --all -- --check
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode docs -SkipRedactionInventory
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode container -SkipRedactionInventory
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package-release.ps1 -WhatIf
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package-release.ps1 -SkipBuild -OutputDir target/package-release-rpd-closeout
git diff --check
```

Result: Pass.

## Follow-Ons

- Recommended next lane: Metadata Provider Breadth.
- If downloads is prioritized: split `managed-import-staging` with quarantine,
  validation, and manual promotion; do not start with torrent/Usenet or broad
  autonomous acquisition.
- Prove Docker image build in CI or a dedicated packaging environment if image
  publication becomes a release requirement.
