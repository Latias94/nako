# PCMH-020 - Playback Decision Matrix

Date: 2026-05-31
Status: Done

## Result

Added playback-only table-driven compatibility tests in
`crates/nako-playback/src/lib.rs`.

The matrix covers Direct Play, Remux, HLS Transcode for unsupported video and
audio codecs, HDR-to-SDR tone-map-required Remux denial, audio downmix-required
Remux denial, and requested HLS output shape propagation.

Audio output requirement cases now cover passthrough, downmix, normalization,
and combined downmix plus normalization.

## Validation

```text
cargo nextest run -p nako-playback compatibility --no-fail-fast
cargo nextest run -p nako-playback hdr audio --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

All gates passed on 2026-05-31. `git diff --check` reported only Windows
line-ending normalization warnings.

## Scope

The task stayed inside `nako-playback`. No transcode, server, Public Client API,
media probe schema, persisted preference, device profile, or web/player changes
were made.
