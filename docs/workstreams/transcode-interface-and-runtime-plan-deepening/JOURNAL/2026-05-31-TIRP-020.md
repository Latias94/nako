# TIRP-020 Session Journal

## Summary

- Added `HlsRuntimePlanRequest` and `HlsRuntimePlan` in `nako-transcode` so HLS execution policy, profile identity, request variant, and request identity are assembled behind the transcode Interface.
- Moved HLS media rendition selection for audio and selected subtitle sidecars into `HlsMediaRenditionPlan::selected_from_probe`.
- Updated server HLS orchestration to call the transcode runtime plan and pass that plan into staging layout creation.

## Scope Notes

- Did not expand HDR tone mapping, subtitle burn-in, hardware capability matrices, HLS lifecycle consolidation, or resource admission policy.
- Did not add a `nako-transcode` dependency on `nako-playback`.

## Evidence

- `cargo nextest run -p nako-transcode hls_runtime_plan --no-fail-fast`: passed.
- `cargo nextest run -p nako-transcode hls audio --no-fail-fast`: passed.
- `cargo nextest run -p nako-server hls --no-fail-fast`: passed.
- `cargo fmt --all -- --check`: passed.
- `git diff --check`: passed with only Windows line-ending warnings.
