# TIRP-010 - Scope And Evidence Freeze

Date: 2026-05-31

## Result

Opened `transcode-interface-and-runtime-plan-deepening` on the
`playback-transcode` lane.

## Decisions

- First executable task is `TIRP-020`, a HLS runtime plan Interface slice.
- `TIRP-030` will ratchet low-level FFmpeg adapter exposure after the deeper
  Interface exists.
- HDR `HTP-030` should remain parked until this workstream is accepted or the
  planner explicitly serializes the shared scope.
- Stage-aware hardware expansion, HLS lifecycle consolidation, resource
  admission unification, and Jellyfin-class capability matrices are follow-ons.

## Validation

```text
python -m json.tool docs/workstreams/transcode-interface-and-runtime-plan-deepening/WORKSTREAM.json
git diff --check -- docs/workstreams/transcode-interface-and-runtime-plan-deepening docs/architecture/PLAYBACK.md docs/architecture/WORKSTREAM_LINKS.md docs/architecture/LANES.md docs/workstreams/README.md
```

Both gates passed on 2026-05-31. `git diff --check` reported only Windows
line-ending warnings.
