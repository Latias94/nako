# 2026-05-25 — Open

Opened the workstream after inspecting the scan and Addon runtime code.

Findings:

- `MetadataScanAcquisitionPlan.addon_scrape` exists but is hard-coded false.
- `LibraryScanAppService` runs NFO import only during scan metadata acquisition.
- Addon registration, routing plan sync, task run creation, and official `bulk-metadata-scrape` declaration already exist.
- Existing Addon Event scheduler/replay changes are dirty and should remain separate from this narrow scan-time task creation lane.
