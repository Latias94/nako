# 2026-05-18 ATGSE-060

Task: close Addon Token Grants Side Effects and split concrete protected writes.

## Summary

The ATGSE lane is complete. Addon Tokens, accepted Addon Permissions,
Library-Scoped Addon Grants, addon-principal runtime authorization, and the
first Addon Side Effect intake proof are implemented and documented.

Concrete Canonical Metadata, Managed Artwork, subtitle, NFO, and Library File
Write application behavior is a separate scope boundary and is now tracked in
`docs/workstreams/addon-protected-writes/`.

## Review Notes

- ATGSE target state is satisfied by the ATGSE-030, ATGSE-040, and ATGSE-050
  slices.
- The remaining work is not missing token/grant/intake behavior; it is concrete
  effect application breadth.
- No ADR amendment is required for the split because ADR 0020 already requires
  Nako-owned APIs, Addon Tokens, accepted permissions, library grants, audit,
  and resource boundaries.
- A future ADR or ADR amendment should be split only if protected writes move
  toward OAuth-first authorization, Public Client write APIs, broad Admin API
  reuse, or direct storage/file authority.

## Next Step

Run APW-020 in `docs/workstreams/addon-protected-writes/` before changing
metadata, artwork, subtitle, NFO, or Library File Write apply behavior.
