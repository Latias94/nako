# MWF-040 - Media Browse, Search, And Detail

Date: 2026-05-26
Status: DONE

## Work Completed

- Added `/media/libraries` with URL-owned pagination state.
- Added `/media/libraries/:libraryId` using `getLibrary()` and
  `listLibrarySources()` from the Public Client SDK.
- Kept library detail honest: it shows source evidence, not a fake
  library-scoped item grid.
- Added `/media/search` with URL-owned `q`, `facet`, `limit`, and `offset`
  state.
- Added `/media/items/:itemId` with item metadata and safe source facts.
- Extended the Media fixture/live data-source boundary with `getLibrary()`,
  `listLibrarySources()`, and item detail coverage.
- Fixed Media sidebar active-state behavior so `/media/libraries/*` does not
  also mark Home active.

## Validation

- `cd apps/admin-web && npm run test -- mediaSurface.test.tsx mediaDataSource.test.ts`
  - Result: passed, 9 tests.
- `cd apps/admin-web && npm run test -- App.test.tsx mediaSurface.test.tsx mediaDataSource.test.ts`
  - Result: passed, 103 tests.
- `cd apps/admin-web && npm run check`
  - Result: passed.
- `cd apps/admin-web && npm run test`
  - Result: passed, 153 tests.
- `cd apps/admin-web && npm run build`
  - Result: passed. Vite retained the existing large chunk advisory.
- `rg -n "admin/v1|AdminApi|adminApi|source_locator|local path|ffmpeg" apps/admin-web/src/surfaces/media`
  - Result: no matches.
- Playwright smoke against `http://127.0.0.1:5174`:
  - Fixture mode entered successfully.
  - `/media/libraries` rendered accessible libraries.
  - `/media/libraries/library-anime` rendered source evidence.
  - `/media/items/item-episode-1` rendered item detail and safe source facts.
  - `/media/search` wrote `q=rain` into the URL after submit.

## Follow-On

- MWF-GAP-002 remains open for a real Public Client library item grid.
- MWF-050 should start with source selection and playback-decision preview.
- Browser playback still requires the MWF-GAP-004 auth transport decision.
