# 2026-05-26 - MWF-020 Public Client API And SDK Readiness

Ran the Public Client OpenAPI and TypeScript SDK readiness checks before
scaffolding Media Web.

Validation:

- `cargo test -p nako-api public_openapi -- --nocapture` passed.
- `cargo run -q -p nako-api --example emit-typescript-sdk -- --output sdk/typescript/src/index.ts` completed and produced no content diff.

Findings:

- Public Client SDK covers libraries, global items, item detail, credits,
  images, people/tags/genres item lists, search, source probes, playback
  decisions, direct/remux/HLS stream routes, playback sessions, and User
  Playback State.
- Media Web still needs conscious handling for current principal/session
  display, library-scoped item browse, Recently Added, and browser playback
  auth transport.
- MWF-030 may scaffold app shell/data-source boundaries, but should not build a
  fake library grid or real browser player before those gaps are resolved or
  explicitly narrowed.

