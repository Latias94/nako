# 2026-05-20

- Opened workstream for Android connection composition cleanup.
- Goal: remove duplicate connection dependency composition from UI and use the
  root environment seam.
- Added `TaruAppEnvironment.createConnectionRuntime()`.
- Removed unused platform-building `TaruConnectionShell` entrypoint.
- Changed `TaruConnectionShellContent` to accept `ConnectionRuntime` and an
  explicit initial snapshot.
- Validation passed:
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.TaruAppCompositionTest --no-daemon --no-parallel`
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.connection.* --tests dev.taru.android.ui.* --no-daemon --no-parallel`
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon --no-parallel`
  - `git diff --check`
