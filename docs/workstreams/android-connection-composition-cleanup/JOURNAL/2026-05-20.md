# 2026-05-20

- Opened workstream for Android connection composition cleanup.
- Goal: remove duplicate connection dependency composition from UI and use the
  root environment seam.
- Added `NakoAppEnvironment.createConnectionRuntime()`.
- Removed unused platform-building `NakoConnectionShell` entrypoint.
- Changed `NakoConnectionShellContent` to accept `ConnectionRuntime` and an
  explicit initial snapshot.
- Validation passed:
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.nako.android.ui.NakoAppCompositionTest --no-daemon --no-parallel`
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.nako.android.ui.connection.* --tests dev.nako.android.ui.* --no-daemon --no-parallel`
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon --no-parallel`
  - `git diff --check`
