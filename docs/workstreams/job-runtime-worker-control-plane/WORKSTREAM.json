{
  "slug": "job-runtime-worker-control-plane",
  "status": "completed",
  "goal": "Design and implement a shared durable job worker/control-plane boundary, starting with Managed Artwork ingest as the first supervised vertical slice",
  "owner": "codex",
  "created_at": "2026-05-19",
  "updated_at": "2026-05-19",
  "current_task": "closed",
  "first_slice": "Opt-in Managed Artwork ingest worker registered through RuntimeSupervisor and sharing the safe artifact pipeline with Admin process-next",
  "non_goals": [
    "distributed multi-process scheduling",
    "automatic retry policy for every job kind in the first slice",
    "migrating all job kinds at once",
    "generic untyped task execution",
    "exposing raw job input, summary, errors, source locators, storage handles, local paths, or secrets"
  ],
  "validation_gates": [
    "Get-Content docs\\workstreams\\job-runtime-worker-control-plane\\WORKSTREAM.json | ConvertFrom-Json",
    "git diff --check",
    "cargo nextest run -p taru-server job_runtime_worker --no-fail-fast",
    "cargo nextest run -p taru-db managed_artwork_ingest --no-fail-fast",
    "cargo nextest run -p taru-server queued_artwork_ingests --no-fail-fast",
    "cargo nextest run -p taru-db managed_artwork_startup_recovery --no-fail-fast",
    "cargo check -p taru-core -p taru-db -p taru-api -p taru-server --tests",
    "cargo fmt --all -- --check"
  ],
  "follow_ons": [
    "metadata maintenance worker migration",
    "webhook delivery worker migration",
    "NFO import/export worker migration",
    "automation worker migration",
    "scan/probe worker migration",
    "durable job cancellation and ownership leases",
    "generic retry/backoff policy",
    "distributed scheduler or multi-process leases"
  ]
}
