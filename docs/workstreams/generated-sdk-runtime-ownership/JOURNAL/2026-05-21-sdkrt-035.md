# SDKRT-035 Journal — UniFFI Compile-Only Scaffold

Date: 2026-05-21
Status: DONE

## Scope

Add the thinnest UniFFI crate over `taru-client-core` and prove the binding
surface compiles before Android app behavior consumes it.

## Implementation

- Added `crates/taru-client-uniffi`.
- Added `cdylib` and `rlib` crate types for native binding use and Rust tests.
- Added UniFFI records/enums mirroring the core tracer's FFI-safe shape.
- Exported:
  - `start_connection_probe(base_url, access_token)`;
  - `advance_connection_probe(base_url, access_token, response)`.
- Kept all behavior in `taru-client-core`; the binding crate only converts
  records and delegates.

## Validation

- `cargo fmt --package taru-client-core --package taru-client-uniffi --check`
  passed after the Rust core and UniFFI scaffold updates.
- `cargo nextest run -p taru-client-uniffi --no-fail-fast` passed with 1 test.
- `python -m json.tool docs/workstreams/generated-sdk-runtime-ownership/WORKSTREAM.json > $null`
  passed.
- `git diff --check` passed with expected line-ending normalization warnings for
  touched workspace/workstream files.

## Boundaries Preserved

- No runtime policy was added to the binding crate.
- No Android app code was changed.
- Binding generation and Android consumption were deferred to `SDKRT-040`.

## Next Task

`SDKRT-040`: wire only the Android connection flow to the binding boundary while
preserving Android-owned transport, token/profile handling, diagnostics, UI, and
Media3 ownership.
