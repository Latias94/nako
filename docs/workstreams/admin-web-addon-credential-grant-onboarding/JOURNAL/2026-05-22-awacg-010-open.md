# 2026-05-22 AWACG-010 Open

Opened `admin-web-addon-credential-grant-onboarding` after Taru's Addon
Operations, Install Guide, and Onboarding flows were pushed to `origin/main`.

Goal: complete the Admin Web bridge from registered disabled Addon to enabled
runtime Addon through one-time token issue/rotation, revoke, accepted grants,
and readiness checklist while preserving the sidecar lifecycle boundary.

AWACG-020 completed in the same session:

- explicit TypeScript contract types for token issue/rotation one-time raw
  token responses and grant replacement;
- Admin Web client methods for issue/rotate/revoke/replace grants;
- data-source action methods returning raw token only as action output;
- focused tests for client route shape and raw-token exclusion from `load()`.
