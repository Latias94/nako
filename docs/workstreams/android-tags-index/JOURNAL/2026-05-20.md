# 2026-05-20

- Opened as the follow-on from the closed Android Relationship Indexes lane.
- Genres Index is complete and smoke-proven; Tags Index should reuse that
  architecture.
- First task is ATI-010: add typed Android client coverage for
  `GET /tags?limit=&offset=`.
- Completed ATI-010:
  - Added `TagListResponse` and `TaruBrowseClient.listTags`.
  - Added focused client tests for request URL construction, bearer auth,
    response decoding, safe diagnostics, and unsupported API version handling.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.browse.TaruBrowseClientTest --no-daemon`.
- Next task is ATI-020: reuse relationship index route state for Tags.
- Completed ATI-020:
  - Added `RelationshipIndexFamily.Tags` and
    `TagListResponse.toRelationshipIndexContent`.
  - `BrowseSession`, navigation save/restore, and
    `ClientBrowseDataSource.loadRelationshipIndex(Tags)` now reuse the
    proven relationship index route state.
  - Relationship index presentation copy is family-aware for Genres and Tags.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.screens.relationship.RelationshipIndexRouteTest --no-daemon --rerun-tasks`.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests "dev.taru.android.ui.browse.*" --no-daemon`.
  - Earlier parallel Gradle validation hit Kotlin incremental cache contention;
    serial reruns passed.
- Next task is ATI-030: add the Home Tags entry point and productize the
  screen entry.
- Completed ATI-030:
  - Home exposes Tags next to Genres as a nested relationship index entry.
  - `TaruBrowseShell` dispatches the Tags anchor through
    `BrowseAction.OpenRelationshipIndex(RelationshipIndexFamily.Tags)`.
  - `RelationshipIndexRoute` keeps the shared screen shape and now uses
    family-aware icons as well as family-aware copy.
  - Added focused host coverage for opening and loading the Tags relationship
    index route.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.browse.BrowseShellHostTest --no-daemon`.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.browse.BrowseShellHostTest --tests dev.taru.android.ui.screens.relationship.RelationshipIndexRouteTest --no-daemon`.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`.
- Next task is ATI-040: verify smoke value and close the lane.
- Completed ATI-040:
  - Added `profile-with-media` smoke coverage for Home -> Tags -> Lighthouse
    -> Related Media Items.
  - Smoke evidence now includes `tag-index` and `tag-index-facet` surfaces.
  - Validation passed:
    `pwsh -NoProfile -File apps\android\scripts\Smoke-Regression.ps1 -States profile-with-media -RetriesPerState 0`.
  - Smoke regression report:
    `apps/android/build/smoke-regression/20260520-164905/report.md`.
  - The fixture server build emitted pre-existing `taru-server` unused-code
    warnings; smoke passed with zero retries.
  - Closed the workstream.
