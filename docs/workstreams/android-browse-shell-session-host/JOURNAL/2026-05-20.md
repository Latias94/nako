# 2026-05-20

- Opened workstream for Android browse shell session host deepening.
- Goal: move remaining shell lifecycle orchestration and saveable-state
  synchronization out of `TaruBrowseShell` without changing user-facing UI.
- Added `BrowseShellHost` and `ClientBrowseShellRuntime`.
- Simplified `TaruBrowseShell` so it no longer constructs `BrowseSession` or
  `SettingsSession` directly and no longer uses route-loading `LaunchedEffect`.
- Removed unused `ClientSettingsRuntime`.
- Validation passed:
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.browse.BrowseShellHostTest --no-daemon --no-parallel`
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.ui.browse.* --no-daemon --no-parallel`
  - `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon --no-parallel`
  - `git diff --check`
