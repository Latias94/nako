# AWPDG-040 Journal

Date: 2026-06-02

Implemented the Web Admin durable Candidate Review governance surface.

Highlights:

- added live Admin data-source methods for Candidate Review detail and apply;
- added fixture read fallback while keeping fixture mutations disabled;
- added `/admin/metadata/candidate-reviews?review_id=<id>`;
- added route-state and data-source contract coverage for live read, redaction,
  two-step apply, idempotency-key submission, replay result rendering, and
  post-apply refetch;
- reduced unbound Admin mock surfaces so the new route stayed within bundle
  budget without raising thresholds.

Validation:

- `npm --prefix web run check`
- `npm --prefix web run test`
- `npm --prefix web run build:budget`
- Playwright smoke on
  `http://127.0.0.1:3001/admin/metadata/candidate-reviews?review_id=smoke-review`

Next:

- run `AWPDG-050` closeout or split follow-ons for related-node hierarchy
  application, provider endpoint depth, and Candidate Review list/navigation.
