# AWPDG-020 Journal

Date: 2026-06-02
Task: AWPDG-020

## Summary

Added read-only Admin API inspection for durable Metadata Candidate Reviews:

- route: `GET /admin/v1/metadata/candidate-reviews/{review_id}`;
- DTOs: `AdminMetadataCandidateReviewResponse`, review detail summaries,
  application plan summaries, and explicit application boundary facts;
- service boundary: `MetadataAppService::get_admin_metadata_candidate_review`;
- contract sync: Rust Admin contract plus generated `apps/admin-web` and
  `web` TypeScript contract outputs.

## Boundary Notes

- The route reuses `build_candidate_review_application_plan` for action,
  reason, source, and existing mapping facts.
- The route does not call `MetadataCandidateReviewApplicationService::apply`.
- The response exposes related graph nodes as preview evidence only.
- Candidate metadata descriptions, tag values, provider bodies, paths, tokens,
  and fingerprints are not exposed.

## Validation

- RED: `cargo nextest run -p nako-server admin_v1_metadata_candidate_review_detail --no-fail-fast`
  failed while Admin Candidate Review DTOs and route were missing.
- GREEN: `cargo nextest run -p nako-server admin_v1_metadata_candidate_review_detail --no-fail-fast`
  passed: 1 test run, 1 passed.
- GREEN: `cargo nextest run -p nako-api admin_contract --no-fail-fast` passed:
  5 tests run, 5 passed.
- GREEN: `cargo nextest run -p nako-server candidate_review admin --no-fail-fast`
  passed: 108 tests run, 108 passed.
- GREEN: `cargo fmt --all -- --check` passed.
- GREEN: `git diff --check` passed with Git CRLF normalization warnings only.

## Next

`AWPDG-030` should add a separate Admin API apply mutation that calls
`MetadataCandidateReviewApplicationService` with explicit stale guards and
idempotency. Keep Web UI and related-node hierarchy application out of that
mutation slice.
