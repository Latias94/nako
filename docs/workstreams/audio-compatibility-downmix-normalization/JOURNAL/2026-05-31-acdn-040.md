# ACDN-040 Journal

Date: 2026-05-31
Status: Done

## Context

Read the workstream metadata, task ledger, handoff, evidence ledger, context
manifest, and playback architecture map before editing. The assigned code
scope was limited to `crates/nako-transcode/src/ffmpeg.rs`,
`crates/nako-transcode/src/lib.rs`, and `crates/nako-transcode/src/tests*`.

## Implementation

- Added command-plan tests proving FFmpeg HLS emits audio filters for downmix,
  normalization, combined downmix plus normalization, and audio sidecar outputs.
- Added HLS audio filter planning from
  `TranscodeExecutionPolicy.audio_output`.
- Used `aformat=channel_layouts=...` for deterministic channel target filters.
- Used `loudnorm=I=-16:TP=-1.5:LRA=11` for deterministic one-pass
  normalization.
- Kept filter order deterministic: downmix before normalization.
- Did not edit HDR tone mapping, subtitle burn-in, web player, public DTO, or
  server playback code.

## Validation

```text
cargo nextest run -p nako-transcode hls audio --no-fail-fast
cargo nextest run -p nako-server hls --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

The transcode and server HLS gates passed on 2026-05-31. One early server HLS
run hit the tool timeout before producing a test result; the same gate passed
when rerun with a longer timeout.
