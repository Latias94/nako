# 2026-05-26 - ASM-030 Through ASM-050

## Summary

Consumed the closed ASCA backend authority and completed the Admin Web
metadata raw cache settings mutation slice.

## Decisions

- The only editable settings group in this lane is metadata raw cache
  retention and startup cleanup.
- PUT persists an Admin desired-state override; Admin Web renders
  `requires_restart` instead of implying hot application.
- Mock fallback remains read-only and cannot expose a fake save path.
- Broader global settings remain blocked until a separate authority lane
  defines source-of-truth, validation, redaction, and runtime semantics.

## Implementation Notes

- Added Admin Web client/data-source methods for
  `GET|PUT /admin/v1/settings/metadata/raw-cache`.
- Added Settings route state for raw-cache draft editing, prepare/confirm save,
  success notice, error notice, and fallback-disabled state.
- Added focused tests for client, data source, Settings UI success/failure, and
  mock fallback behavior.
- Fixed the raw-cache mutation panel mobile layout after browser smoke found a
  small overflow at 390px width.

## Evidence

See `EVIDENCE_AND_GATES.md` for command output summaries and browser smoke
results.
