# SHR-030 PostgreSQL Contract Harness And CI Shape

Date: 2026-05-21

## Work Completed

- Added `scripts/postgres-contract-harness.ps1`.
- Added `scripts/postgres-contract-harness.sh`.
- Routed `scripts/release-gate.ps1 -Mode postgres` and
  `scripts/release-gate.sh --mode postgres` through the harness.
- Added `.github/workflows/release-gate.yml` with:
  - fast release gate job;
  - PostgreSQL Managed Artwork contract job using a PostgreSQL service.
- Documented PostgreSQL harness behavior and cleanup boundaries.

## Validation

- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/postgres-contract-harness.ps1 -Suite managed-artwork`
  - Started local PostgreSQL 17 under `target/postgres-contract`.
  - Ran ignored PostgreSQL Managed Artwork contracts.
  - Passed 6/6 tests.
  - Stopped PostgreSQL and removed `target/postgres-contract`.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode postgres -SkipRedactionInventory`
  - Passed format and diff gates.
  - Delegated to the PostgreSQL harness.
  - Passed PostgreSQL Managed Artwork contracts 6/6.
- Missing-tooling simulation with no `NAKO_TEST_POSTGRES_URL` and restricted
  `PATH`:
  - Printed a clear skip for missing `initdb`, `pg_ctl`, and `createdb`.
  - Exited 0.
- `Test-Path target/postgres-contract`
  - Returned `False` after harness runs.
- `cargo fmt --all -- --check`
  - Passed.
- `git diff --check`
  - Passed.

## Notes

- The local machine has PostgreSQL 17 tooling available in
  `F:\MySoftware\PostgreSQL\17\bin`.
- Shell script execution still needs Linux/Git Bash evidence; the Windows
  `bash.exe` in this environment is the WSL shim and previously timed out.
- SHR-040 should compose API/OpenAPI/SDK/redaction checks into the release
  gate.
