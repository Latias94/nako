# 2026-05-21 — SHR-010 Baseline

## Summary

Opened the Self-Hosted Release Readiness workstream.

## Findings

- Worktree was clean on `main`.
- No repo-root `.github/`, `scripts/`, `ci`, `.cargo`, `.config`, or
  `nextest.toml` exists.
- `Cargo.toml` defines a Rust workspace over `crates/*`.
- The next work is not another feature lane; it is release composition:
  local/CI gates, PostgreSQL harness, API/SDK/redaction checks, deployment
  examples, backup/restore runbooks, and self-host smoke.

## Next

Run SHR-020 to add the first local release gate entrypoint.
