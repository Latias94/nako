# SHR-070 — End-To-End Self-Host Smoke

Date: 2026-05-21
Status: DONE

## Work Completed

- Added `crates/nako-server/src/http/tests/self_host_smoke.rs`.
- Registered the smoke module in `crates/nako-server/src/http/tests/mod.rs`.
- Reused the Addon artwork test helpers across sibling smoke tests with
  `pub(super)` visibility instead of duplicating helper logic.
- Added `scripts/self-host-smoke.ps1` and `scripts/self-host-smoke.sh`.
- Added the SQLite smoke to `scripts/release-gate.*` fast/API gate coverage.
- Added a CI job shape for the SQLite self-host smoke.

## Smoke Coverage

The SQLite HTTP smoke covers:

- `/health`,
- library scan job enqueue,
- NFO export job enqueue,
- metadata refresh job enqueue,
- Addon artwork proposal and admin candidate acceptance,
- Managed Artwork ingest processing and publication,
- public image listing and byte serving,
- playback decision and byte-range streaming,
- Admin overview/system diagnostics redaction assertions.

The PostgreSQL path delegates to the Managed Artwork contract harness because
that is the stateful self-hosted PostgreSQL artifact path that needs adapter
parity.

## Validation

```text
cargo nextest run -p nako-server self_host_smoke --no-fail-fast
```

Result: pass, 1/1.

```text
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/self-host-smoke.ps1 -Backend sqlite
```

Result: pass, 1/1.

```text
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/self-host-smoke.ps1 -Backend postgres
```

Result: pass, PostgreSQL Managed Artwork contracts 6/6 and
`target/postgres-contract` cleaned.

```text
cargo fmt --all -- --check
git diff --check
```

Result: pass.

## Handoff

Continue with SHR-080 closeout verification.
