# SHR-040 API, SDK, And Redaction Gate

Date: 2026-05-21

## Work Completed

- Extended `scripts/release-gate.ps1 -Mode api`.
- Extended `scripts/release-gate.sh --mode api`.
- Added the CI `api-sdk-redaction` job to `.github/workflows/release-gate.yml`.
- Composed Public OpenAPI, Public TypeScript SDK, Admin Web TypeScript
  contract, Rust client, public protocol, dependency inventory, and redaction
  checks into one gate.

## Validation

- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode api`
  - Redaction inventory wrote 3148 matches.
  - Passed `cargo check -p taru-server --tests`.
  - Passed `cargo check -p taru-api --tests`.
  - Passed `cargo check -p taru-client --tests`.
  - Passed `cargo check -p taru-client-protocol --tests`.
  - Passed `cargo nextest run -p taru-api openapi --no-fail-fast`: 6/6.
  - Passed `cargo nextest run -p taru-api sdk --no-fail-fast`: 6/6.
  - Passed `cargo nextest run -p taru-api admin_contract --no-fail-fast`: 5/5.
  - Passed `cargo nextest run -p taru-client --no-fail-fast`: 9/9.
  - Passed `cargo nextest run -p taru-client-protocol --no-fail-fast`: 8/8.
  - Ran `cargo tree -p taru-client`.
  - Ran `cargo tree -p taru-client-protocol`.
  - Passed `npm run generate --prefix sdk/typescript`.
  - Passed `npm run check --prefix sdk/typescript`.
  - Passed `npm run generate:admin-api --prefix apps/admin-web`.
  - Passed `npm run check --prefix apps/admin-web`.
  - Passed Managed Artwork API/server redaction tests: `taru-api` 12/12 and
    `taru-server` 13/13.
- `git diff -- sdk/typescript/src/index.ts apps/admin-web/src/adminApi/generated/contract.ts`
  - No diff; generated tracked artifacts were already synchronized.
- `cargo fmt --all -- --check`
  - Passed.
- `git diff --check`
  - Passed.

## Notes

- `release-gate fast` now includes the API/SDK/redaction gate, making the fast
  path more release-trustworthy but heavier.
- SHR-050 should add self-hosted SQLite/PostgreSQL deployment examples and
  operator configuration guidance.
