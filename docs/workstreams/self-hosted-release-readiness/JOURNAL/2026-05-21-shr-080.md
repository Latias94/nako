# SHR-080 — Closeout

Date: 2026-05-21
Status: DONE

## Closeout Decision

Close `self-hosted-release-readiness` as completed.

The lane now has:

- local release gate entrypoints for PowerShell and shell,
- `.config/nextest.toml`,
- local PostgreSQL contract harnesses,
- GitHub Actions release-gate job shape,
- API/SDK/redaction gate composition,
- SQLite/PostgreSQL deployment examples,
- backup/restore/upgrade runbook,
- SQLite self-host smoke over public/Admin HTTP boundaries,
- PostgreSQL Managed Artwork contract smoke equivalent,
- fresh closeout verification evidence.

## Verification

```text
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode docs
```

Result: pass. Format/diff gates passed and redaction inventory wrote 3208
matches.

```text
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode fast
```

Result: pass. SQLite Managed Artwork contracts passed 6/6; API/OpenAPI/Admin
contract/client/protocol/TypeScript/Admin Web gates passed; Managed Artwork API
tests passed 12/12; Managed Artwork server tests passed 13/13; SQLite
self-host smoke passed 1/1.

```text
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode postgres -SkipRedactionInventory
```

Result: pass. PostgreSQL Managed Artwork contracts passed 6/6 through the local
PostgreSQL 17 harness.

```text
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode workspace -SkipRedactionInventory
```

Result: pass. `cargo check --workspace --tests` passed; `cargo nextest run
--workspace --no-fail-fast` passed 506/506 with 25 skipped.

## Follow-Ons

Do not reopen this lane for unrelated product scope. Open separate workstreams
for:

- AI product features,
- network traversal/tunneling,
- Native Plugin ABI,
- provider breadth,
- SDK/package publishing,
- release packaging/container image hardening.

## Residual Risk

Shell script execution was not proven in Git Bash on this Windows machine
because the available `bash.exe` was the WSL shim. The Linux CI job shape is
present and should provide the shell-script evidence once pushed.
