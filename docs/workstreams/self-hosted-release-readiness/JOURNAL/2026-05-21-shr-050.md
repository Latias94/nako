# SHR-050 Self-Hosted Deployment Examples

Date: 2026-05-21

## Work Completed

- Added `deploy/sqlite/nako.toml`.
- Added `deploy/postgres/nako.toml`.
- Added `deploy/compose/postgres.yml`.
- Added `docs/deployment/SELF_HOSTED.md`.
- Linked the deployment guide from `docs/README.md`.

## Validation

- Python `tomllib` parsed both TOML example configs.
- `docker compose -f deploy/compose/postgres.yml config` with no
  `NAKO_POSTGRES_PASSWORD` failed safely with the required-variable message.
- `NAKO_POSTGRES_PASSWORD=example-not-a-secret docker compose -f
  deploy/compose/postgres.yml config` rendered the PostgreSQL service.
- Required-topic grep covered `database_url`, `artifact_root`, `staging`,
  `auth`, `Addon`, `Webhook`, `Playback Runtime`, `diagnostics`, `SQLite`, and
  `PostgreSQL`.
- `cargo fmt --all -- --check`
  - Passed.
- `git diff --check`
  - Passed.

## Notes

- Examples bind to loopback and keep auth enabled by default.
- The PostgreSQL compose example provides only the database service. Nako itself
  remains source-built in this release baseline until a future packaging lane
  defines a first-party image.
- SHR-060 should document backup, restore, migration, upgrade, and state
  boundary procedures.
