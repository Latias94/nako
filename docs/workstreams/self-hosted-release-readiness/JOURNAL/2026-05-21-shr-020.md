# SHR-020 Local Release Gate Entry Point

Date: 2026-05-21

## Work Completed

- Added `scripts/release-gate.ps1` as the Windows/PowerShell-first local release
  gate.
- Added `scripts/release-gate.sh` as the Linux/macOS/CI shell entrypoint.
- Added `.config/nextest.toml` with shared nextest output and fail-fast
  posture.
- Documented `docs`, `fast`, `db`, `api`, `postgres`, `workspace`, and `all`
  gate modes.
- Routed redaction inventory output to
  `target/release-gate/redaction-inventory.txt`.

## Validation

- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode docs`
  - Passed format and diff checks.
  - Wrote 3148 redaction inventory matches.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode fast`
  - Passed format and diff checks.
  - Wrote 3148 redaction inventory matches.
  - Passed `cargo check -p taru-db --tests`.
  - Passed SQLite Managed Artwork contracts: 6/6.
  - Passed `cargo check -p taru-api --tests`.
  - Passed `cargo check -p taru-server --tests`.
  - Passed `taru-api managed_artwork`: 12/12.
  - Passed `taru-server managed_artwork`: 13/13.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/release-gate.ps1 -Mode postgres -SkipRedactionInventory`
  - Passed format and diff checks.
  - Safely skipped PostgreSQL contracts without `TARU_TEST_POSTGRES_URL`.
- `cargo fmt --all -- --check`
  - Passed.
- `git diff --check`
  - Passed.
- `cargo nextest show-config version`
  - Passed and confirmed nextest can load workspace config.

## Notes

- The shell script still needs Linux/Git Bash execution evidence. In this
  Windows environment, `bash` resolved to the WSL shim and timed out during
  syntax probing; Git Bash was not installed.
- SHR-030 should turn the PostgreSQL safe-skip path into a first-party harness
  and CI-ready job.
