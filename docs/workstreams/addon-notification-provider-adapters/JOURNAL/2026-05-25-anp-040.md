# 2026-05-25 — ANP-040 Integration And Docs

Task: ANP-040
Status: DONE

## Work Completed

Updated the default notification bridge smoke to assert the event ACK reports
`http_webhook` as disabled when no provider configuration is supplied.

Updated operator-facing packaging notes to make the default smoke boundary
explicit: default smoke does not require live webhook credentials, while
fixture-backed Rust tests cover provider sends.

Confirmed the runtime manifest shape and official catalog facts remain
unchanged. No Nako core provider concepts, provider secrets, or provider URL
storage were introduced.

## Validation

Passed:

```powershell
pwsh -File addons/notification-bridge/smoke.local.ps1 -SidecarBaseUrl http://127.0.0.1:19110
cargo nextest run -p nako-notification-bridge --no-fail-fast
cargo check -p nako-notification-bridge --tests
cargo fmt --all -- --check
cargo nextest run -p nako-official-addon-catalog notification_bridge --no-fail-fast
python -m json.tool docs\workstreams\addon-notification-provider-adapters\WORKSTREAM.json > $null
git diff --check
```

Official addon nextest ran 14 tests. Focused host catalog gate ran 2 tests.
`git diff --check` emitted only Windows CRLF conversion warnings from the dirty
worktrees.

## Host Gate Decision

No broader host gate is required for this task. ANP-040 did not change the
Addon Protocol, host scheduler, manifest schema, or Nako-owned provider
concepts. The focused catalog test was run to verify the unchanged official
manifest facts still match expectations.

## Next

Continue with ANP-050 closeout: review the lane, record final fresh gates, and
split any remaining provider breadth into named follow-ons.
