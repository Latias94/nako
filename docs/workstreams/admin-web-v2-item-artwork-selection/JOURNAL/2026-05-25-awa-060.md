# AWA-060 Verification And Browser Smoke

Date: 2026-05-25

## Claim

The item artwork selection lane has fresh full Admin Web gate evidence and
browser smoke coverage for item detail, artwork gallery, select confirmation,
and unpublish confirmation.

## Commands

- `npm run check` passed.
- `npm run test` passed: 4 test files, 110 tests.
- `npm run build` passed. Vite emitted the existing large-chunk warning for
  the bundled application asset.
- `git diff --check` passed with existing LF-to-CRLF working-copy warnings only.

## Browser Smoke

Playwright CLI smoke used the local Vite app at
`http://127.0.0.1:5176` and mocked Admin artwork gallery/select/unpublish
responses for the action path.

Desktop `1440x1000`:

- `/items/item-unknown-1` rendered item detail fallback data and the
  `Open Artwork Gallery` link.
- `/items/item-unknown-1/artwork?limit=20&offset=0` rendered live mocked
  artwork gallery data.
- `Prepare select artifact-backdrop-1` then
  `Confirm select artifact-backdrop-1` rendered `Selection updated`.
- `Prepare unpublish poster` then `Confirm unpublish poster` rendered
  `Selection unpublished`.
- Browser console reported no errors.
- Document-level horizontal overflow check passed.
- Unsafe artwork source/storage/path/hash/token text check passed.

Mobile `390x844`:

- Gallery route remained nonblank.
- `scrollWidth=390`, `clientWidth=390`; no document horizontal overflow.
- Browser console reported no errors.
- Unsafe artwork source/storage/path/hash/token text check passed.

## Handoff

Continue with AWA-070 closeout. Review the lane, split remaining work if any,
create `CLOSEOUT.md`, update status fields, and recommend the next Admin Web
V2 lane.
