# APIC-030 Session Journal

Date: 2026-05-19

## Goal

Productize Android artwork fallback rules across Home, Libraries, Detail, and
Player while staying inside the v2 Material 3 Expressive implementation
baseline.

## Work Completed

- Added shared poster, backdrop, and player artwork slot components.
- Replaced scattered poster/backdrop placeholders in Browse and Detail with the
  shared slot components.
- Added a player preparing/error backdrop that is deterministic and local to
  the title. The player still stays video-first and disables Media3 embedded
  artwork display.
- Added unit coverage for fallback initial/kind normalization and player
  fallback title behavior.

## Evidence

Validation:

```powershell
apps\android\gradlew.bat -p apps\android --stop
apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon
pwsh -NoProfile -File apps\android\scripts\Smoke-Regression.ps1 -States profile-with-media
```

Result: PASS on 2026-05-19.

Smoke report:

- `apps/android/build/smoke-regression/20260519-131218/report.md`

Screenshots reviewed:

- `home.png`
- `detail.png`
- `player.png`

## Notes

- A first validation attempt ran unit tests and smoke in parallel. Gradle hit a
  Kotlin incremental cache race while smoke completed successfully. Gradle
  daemons were stopped and the unit test gate was rerun serially.
- No fake artwork, token values, provider storage hints, or filesystem paths
  were introduced in the artwork presentation.
