# APIC-020 Journal

Date: 2026-05-19
Task: APIC-020
Status: DONE

## Summary

Implemented Android selected artwork consumption for Public Client API image
refs. Android now decodes public image refs, fetches visible-page item images
best-effort, constructs active-server-scoped authenticated `/images/{image_id}`
requests, and renders selected artwork in Home, Libraries, and Detail surfaces.

## Implementation Notes

- Added Coil Compose and OkHttp network dependencies.
- Replaced the stale Android `ImageAssetDto` mirror with `PublicImageRefDto`.
- Added `ImagesResponse` and `TaruBrowseClient.itemImages`.
- Added `PublicArtworkSource`, `PublicArtworkRequest`,
  `preferredPublicArtwork`, and `kindWireValue`.
- Added `TaruArtworkImage` as the UI boundary around authenticated Coil
  loading and fallback behavior.
- Kept Search and Facet result rows on fallback artwork unless later enriched
  by route-specific image refs.

## Validation

```powershell
apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon
```

Result: PASS.

## Follow-On

- APIC-030 can polish fallback artwork and visual screenshots.
- APIC-040 can decide whether Library Detail and library source inventory are
  next route coverage priorities.
