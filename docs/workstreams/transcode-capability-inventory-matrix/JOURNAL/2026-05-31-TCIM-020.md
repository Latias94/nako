# TCIM-020 - Bitstream Filter Inventory Baseline

Date: 2026-05-31
Status: Done

## Result

Added optional bitstream-filter stage evidence for `h264_mp4toannexb` in
`nako-transcode` capability reports.

Static CPU and hardware detector reports now expose optional bitstream-filter
stage capability. Probe-derived reports can also represent a missing optional
bitstream filter without making the selected accelerator unavailable.

## Validation

```text
cargo nextest run -p nako-transcode hardware --no-fail-fast
cargo nextest run -p nako-transcode probe --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

All gates passed on 2026-05-31. `git diff --check` reported only Windows
line-ending normalization warnings.

## Scope

The task stayed inside `nako-transcode` inventory/report seams. It did not
change HLS pipeline selection, FFmpeg command planning, server routes, Public
Client DTOs, release packaging, or runtime admission.

## Follow-on

`TCIM-030` should broaden inventory facts for decoders, encoders, filters,
tone-map filters, and subtitle/burn-in-related filters without consuming those
facts in pipeline selection.
