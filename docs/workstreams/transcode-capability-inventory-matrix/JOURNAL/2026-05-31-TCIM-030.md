# TCIM-030 Journal

Date: 2026-05-31
Status: Done

## Scope

Extended `nako-transcode` capability inventory/report evidence for broader
decoder, encoder, filter, tone-map, and subtitle burn-in facts. Kept all new
facts optional and evidence-only.

## Implementation Notes

- Added `tone_map` and `subtitle_burn_in` hardware pipeline report stages.
- Added optional probe-derived facts for HEVC/AV1 decoders, future CPU and
  hardware encoders, common filters, tone-map filters, and subtitle burn-in
  filters.
- Preserved the existing HLS availability rules by keeping broader facts
  `required=false`.
- Did not edit `pipeline.rs`, `ffmpeg.rs`, server routes, Public Client DTOs,
  or release packaging.

## Validation

```text
cargo nextest run -p nako-transcode hardware --no-fail-fast
cargo nextest run -p nako-transcode probe --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

## Follow-Ons

- Keep hardware tone-map execution and vendor-specific command planning in
  dedicated follow-ons.
- Keep subtitle burn-in execution behavior outside this inventory task.
- Let `TCIM-040` handle final review, closeout, and follow-on split decisions.
