# RSHC-040 Journal - Operator Diagnostics And Reset

Date: 2026-05-31
Status: DONE

## Summary

Implemented Admin operator diagnostics and reset for durable **Storage Backend
Health**.

- Added redaction-safe Admin DTOs for durable backend health and reset results.
- Added paginated Admin TypeScript contract entries and refreshed generated
  Admin contract files.
- Added `StorageDiagnosticsAppService` methods for durable health list/reset.
- Added Admin routes:
  - `GET /admin/v1/storage/backends`
  - `POST /admin/v1/storage/backends/{backend_key}/circuit-breaker/reset`
- Added a system test proving Admin can list durable health, redact raw storage
  details, and reset an open **Storage Circuit Breaker** through the durable
  repository contract.

## Scope Notes

- Used `NakoApp::storage()` -> `StorageDiagnosticsAppService`.
- Did not expose raw `NakoDatabase` from `NakoApp` or Admin HTTP.
- Did not change playback staging, cache repair, hash escalation, scan
  scheduling, durable jobs, or schema migrations.

## Validation

```text
cargo nextest run -p nako-server admin_v1_storage --no-fail-fast
cargo nextest run -p nako-server storage_health --no-fail-fast
cargo nextest run -p nako-api --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

All commands passed. `git diff --check` reported only Windows line-ending
normalization warnings.
