# RSHC-030 - Runtime policy adapter

Date: 2026-05-30

## Scope

- Owned `crates/nako-server/src/app/storage.rs`.
- Owned storage app coverage in `crates/nako-server/src/app/tests/storage.rs`.
- Did not change playback staging, scan scheduling, durable jobs, cache repair,
  Admin routes, or schema.

## Changes

- Added stable backend health keys for server `LibraryStorageBackend`
  instances.
- Persisted durable **Storage Backend Health** records on storage runtime
  success and failure outcomes.
- Consulted durable open **Storage Circuit Breaker** state before bounded
  storage work.
- Kept process-local diagnostics/backoff in place for existing diagnostics and
  immediate in-process suppression.

## Reviewer Follow-up

- Initial RSHC-030 implementation gated read-like storage work but missed
  `write_string`, `write`, `plan_link`, `apply`, `cleanup`, and `restore`.
- Added regression assertions proving those paths return `RateLimited` and do
  not touch the wrapped backend while durable circuit state is open.
- Added the same admission gate to the missed methods.

## Evidence

- Added
  `storage_health_records_runtime_updates_and_rejects_durable_circuit`, which
  proves retryable timeout recording, cross-instance durable circuit rejection
  across read and mutation-style runtime work, and successful recovery to
  `Healthy`/`Closed`.

## Validation

```text
cargo nextest run -p nako-server storage_health --no-fail-fast
cargo nextest run -p nako-server storage --no-fail-fast
cargo fmt --all -- --check
git diff --check
```

All commands passed on 2026-05-30. `git diff --check` reported only Windows
line-ending normalization warnings.
