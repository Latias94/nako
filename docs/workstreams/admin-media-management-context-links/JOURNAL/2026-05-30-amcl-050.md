# AMCL-050 - Cross-Surface Verification

Date: 2026-05-30
Status: DONE

## Summary

AMCL-050 completed the verification slice for Management Context Links across
the current `web/` product frontend. No web code changes were needed.

## Validation

```powershell
npm --prefix web ci
npm --prefix web run test
npm --prefix web run check
npm --prefix web run build:budget
rg -n "@/src/api/admin|createAdminMutationDataSource|Admin.*Dto" web\src\features\media web\src\api\public
```

Results:

- `npm --prefix web run test`: passed, 96 tests across 9 files.
- `npm --prefix web run check`: passed.
- `npm --prefix web run build:budget`: passed, `total-js` gzip 331.77 KiB
  under the 335 KiB ceiling.
- Media/Public import guard: no Admin API or mutation-client matches.

## Browser Smoke

The Codex Browser `node_repl` execution tool was not exposed in this session,
so browser smoke used Playwright CLI against a dedicated Vite server on
`http://127.0.0.1:3001`.

Checked routes and transitions:

- `/media/detail?id=live-movie&type=movie`
- Media detail refresh metadata link into `/admin/libraries`
- `/admin/libraries?library_id=library-a&item_id=live-movie&media_type=movie&source_id=file:///mnt/private/source.mkv&intent=scan_library`
- Admin return link back to `/media/detail?id=live-movie&type=movie`
- `/media/library?id=movies`
- Media library scan link into `/admin/libraries?library_id=movies&intent=scan_library`

Screenshots:

- `target/amcl050-media-detail.png`
- `target/amcl050-admin-refresh.png`
- `target/amcl050-admin-return-links.png`
- `target/amcl050-media-library.png`
- `target/amcl050-library-scan-admin.png`

## Result

AMCL-050 is complete. The verified behavior keeps backend link state
authoritative, keeps Media/Public away from Admin mutation clients, and avoids
rendering unsafe `source_id` file URLs while preserving stable Admin/Media
return links.
