# ACFH-090 Closeout Journal

Date: 2026-05-29
Status: DONE

## Work Completed

- Closed the Android client follow-on hardening lane.
- Fixed final validation drift in the Android playback public SDK boundary:
  - `ClientPlaybackDecisionReason` is mapped through `wireValue`;
  - `denied` playback decisions and denial details are represented explicitly;
  - the removed client transcode hardware-acceleration field is no longer part
    of the Android playback model.
- Refreshed Android API fixtures for the current SDK contract:
  - playback decision responses include `denial`, `report`, and `target`;
  - media stream fixtures include `disposition` and `origin`;
  - metadata profile fixtures include `scan`.

## Validation

- PASS:
  `apps\android\gradlew.bat -p apps\android --no-daemon --no-parallel --console=plain :app:testDebugUnitTest --tests "dev.nako.android.connection.*" --tests "dev.nako.android.ui.screens.player.*"`
- PASS:
  `apps\android\gradlew.bat -p apps\android --no-daemon --no-parallel --console=plain :app:testDebugUnitTest`
- PASS:
  `apps\android\gradlew.bat -p apps\android --no-daemon --no-parallel --console=plain :app:assembleDebug -PnakoRustAndroidAbis=x86_64`

## Notes

- Offline Gradle validation was not accepted as final evidence because the
  local offline cache was missing the Android Gradle plugin.
- The first x86_64 assemble attempt failed because the Rust target
  `x86_64-linux-android` was missing; it passed after
  `rustup target add x86_64-linux-android`.
- End-to-end smoke remains environment-blocked from ACFH-020 and is not claimed
  PASS.
