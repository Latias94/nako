# 2026-05-20

- Opened the Android Presentation Runtime Adapters workstream as the first
  follow-on after Android Browse UDF closeout.
- Scope deliberately excludes Media3/player lifecycle ownership, which belongs
  to `android-player-session-architecture`.
- Completed APRA-020 through APRA-040:
  - introduced `ArtworkRequestResolver` and token-vault production adapter;
  - removed raw token/profile artwork props from detail presentation;
  - added `PlayerRouteRenderer` so Browse shell does not know player internals.
- Closed APRA after final Android unit tests and `git diff --check` passed.
