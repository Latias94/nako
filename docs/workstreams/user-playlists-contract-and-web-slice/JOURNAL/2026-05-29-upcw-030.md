# UPCW-030 Session Notes

Date: 2026-05-29
Task: UPCW-030
Status: DONE

## Changes

- Added core User Playlist records, private visibility, write requests, and
  `UserPlaylistRepository`.
- Added SQLite/PostgreSQL baseline schema and repository adapters for
  principal-scoped playlists and ordered membership rows.
- Added NakoDatabase facade forwarding for the playlist repository.
- Added `UserPlaylistAppService` with name validation, media item existence
  checks, stale `expected_version` conflicts, idempotent add, remove, delete,
  and full reorder validation.
- Wired the app-service into `NakoApp` composition for UPCW-040 routes.

## Validation

- `cargo nextest run -p nako-db playlist --no-fail-fast`
- `cargo nextest run -p nako-server user_playlist --no-fail-fast`
- `cargo fmt --all -- --check`
- `python -m json.tool docs/workstreams/user-playlists-contract-and-web-slice/WORKSTREAM.json`
- `git diff --check -- docs/workstreams/user-playlists-contract-and-web-slice crates/nako-core crates/nako-db crates/nako-server/src/app`

## Next

Start UPCW-040. Add Public Client HTTP routes, effective Library Access
filtering for playlist item responses, DTO mapping, and Rust client support.
