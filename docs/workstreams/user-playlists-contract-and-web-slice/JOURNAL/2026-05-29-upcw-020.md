# UPCW-020 Session Notes

Date: 2026-05-29
Task: UPCW-020
Status: DONE

## Changes

- Added `CONTRACT.md` for the frozen current-user private User Playlist Public
  Client contract.
- Added `/users/me/playlists` route inventory entries to
  `nako-client-protocol`.
- Added User Playlist protocol DTOs for summaries, item rows, mutation
  requests, delete responses, and private visibility.
- Added OpenAPI paths/schemas and tests proving User Playlist is not catalog
  Collection, HLS transport playlist, or principal-exposing user state.
- Regenerated TypeScript and Kotlin SDK package entries with playlist DTOs and
  route helpers.
- Updated `docs/api/HTTP_API.md`, `CONTRACT_READINESS.md`, task evidence, and
  handoff notes.

## Validation

- `cargo nextest run -p nako-client-protocol playlist --no-fail-fast`
- `cargo nextest run -p nako-api playlist --no-fail-fast`
- `cargo nextest run -p nako-api sdk --no-fail-fast`
- `cargo nextest run -p nako-client sdk_inventory --no-fail-fast`
- `npm run check --prefix sdk/typescript`
- `cargo fmt --all -- --check`
- `python -m json.tool docs/workstreams/user-playlists-contract-and-web-slice/WORKSTREAM.json`
- `git diff --check -- docs/workstreams/user-playlists-contract-and-web-slice docs/api/HTTP_API.md crates/nako-client-protocol crates/nako-api sdk/typescript sdk/kotlin`

## Next

Start UPCW-030. Add principal-scoped persistence and app-service validation
before any Public Client HTTP route implementation or `web/` playlist UI.
