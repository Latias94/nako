# PGR-050 Session Journal

Date: 2026-05-20
Task: PGR-050 — Library scan commit PostgreSQL parity

## Work Completed

- Added backend-neutral `ScanCommit` contract cases.
- Proved the successful Library scan source commit path across SQLite and
  PostgreSQL:
  - scan snapshot begin/complete;
  - directory snapshot persistence;
  - Media Item and Media Source persistence;
  - Source State persistence/listing;
  - Library Item State persistence;
  - Local Inference Evidence round-trip/listing;
  - Search Projection side effects through `SearchIndex::search`;
  - ingestion failure resolution;
  - Media Technical Facts round-trip through `MediaProbeRepository`.
- Proved transaction rollback for `commit_library_scan_source` when Search
  Projection persistence fails.
- Expanded PostgreSQL proof migration with scan, source-state, search,
  local-inference, ingestion-failure, media-probe, and stream tables.
- Implemented the PostgreSQL repository slices needed by the contract.

## Validation

```bash
cargo fmt --all
cargo check -p nako-db --tests
cargo check -p nako-library --tests
cargo nextest run -p nako-db scan_commit --no-fail-fast
cargo nextest run -p nako-db scan_commit --run-ignored ignored-only --no-fail-fast
cargo nextest run -p nako-db contract --no-fail-fast
cargo nextest run -p nako-db contract --run-ignored ignored-only --no-fail-fast
git diff --check
```

Results:

- nako-db check passed.
- nako-library check passed.
- SQLite scan commit contracts passed: 2 passed.
- PostgreSQL scan commit contracts passed: 2 passed.
- SQLite/default contract run passed: 9 passed.
- PostgreSQL ignored contract run passed: 8 passed.
- `git diff --check` passed with CRLF normalization warnings only.

## Notes

- Media Technical Facts remain a separate repository operation rather than part
  of `LibraryScanSourcePersistenceCommit`; this matches the current domain
  model and avoids forcing probe payloads into the scan commit object.
- PGR-060 should start from SQLite metadata/catalog commit behavior and add
  contracts before expanding PostgreSQL schema further.
