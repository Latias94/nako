# 2026-05-21 — Metadata Provider Breadth Closeout

## Summary

Completed the first post-RPD execution lane. The implementation adds provider
capabilities, deterministic matching decisions, non-destructive ambiguous search
refresh, and a cross-provider candidate review boundary.

## Code Boundaries

- `taru-metadata`
  - provider capability model and provider implementations;
  - `MetadataCandidateMatchingPolicy` and conflict review model;
  - refresh integration that blocks needs-confirmation/rejected search matches
    before fetch/cache/commit.
- `taru-api`
  - diagnostics DTOs for provider capabilities and candidate review output.
- `taru-server`
  - `/metadata/providers` capability diagnostics;
  - `/items/{item_id}/metadata/candidates` candidate review route.

## Verification

- `cargo nextest run -p taru-api --no-fail-fast` — pass.
- `cargo nextest run -p taru-metadata --no-fail-fast` — pass.
- `cargo nextest run -p taru-server metadata --no-fail-fast` — pass.
- `cargo fmt --all -- --check` — pass.
- `git diff --check` — pass with CRLF warnings only.

## Follow-Ons

- Durable candidate review queue and Admin UI accept/reject workflow.
- Query ergonomics for multiple provider overrides in candidate review.
- Next execution lane should be `nfo-link-authority`; playback/transcode ops
  can run in parallel if kept disjoint.
