# Android Playback Session Integrity - TODO

Status: Closed
Last updated: 2026-05-19

## M0 - Scope And Contract Freeze

- [x] APSI-010 [owner=planner] [deps=none] [scope=docs/workstreams/android-playback-session-integrity]
  Goal: Open the lane and freeze the first session-integrity target.
  Validation: workstream docs exist and agree.
  Review: confirm Direct Play remains sessionless and HLS/remux session work
  stays on Public Client API boundaries.
  Evidence: `DESIGN.md`, `TODO.md`, `EVIDENCE_AND_GATES.md`, `WORKSTREAM.json`.
  Handoff: First executable task is APSI-020.

## M1 - Public Session Identity Bridge

- [x] APSI-020 [owner=codex] [deps=APSI-010] [scope=crates/nako-api,crates/nako-client,crates/nako-server,apps/android/app/src/main/java/dev/nako/android/playback,apps/android/app/src/main/java/dev/nako/android/player,apps/android/app/src/main/java/dev/nako/android/ui/browse]
  Goal: Carry remux/HLS playback session identity through a public client
  contract and into Android `PlaybackLaunchRequest`.
  Validation: focused Rust tests for public DTO/OpenAPI/client contract;
  focused Android unit tests for playback target/session propagation.
  Review: no Android parsing of HLS playlist text, local paths, server logs, or
  admin-only diagnostics.
  Evidence: `EVIDENCE_AND_GATES.md` plus focused test output on 2026-05-19.
  Handoff: Next task is APSI-030 once Android can launch with a non-null
  remux/HLS session id.

## M2 - Android Smoke Session Evidence

- [x] APSI-030 [owner=codex] [deps=APSI-020] [scope=apps/android/scripts/Smoke-Emulator.ps1,apps/android/scripts/Smoke-Regression.ps1,apps/android/SMOKE_FIXTURES.md,apps/android/README.md]
  Goal: Add token-safe smoke evidence that proves a non-Direct playback session
  was created, read through Public Client API, and observed after player exit.
  Validation: focused smoke command for the new session fixture path; script
  parse; `git diff --check`.
  Review: smoke artifact must redact tokens and avoid local filesystem paths.
  Evidence: `profile-with-media-session-readback.txt` generated by
  `Smoke-Emulator.ps1 -FixtureState profile-with-media` on 2026-05-19.
  Handoff: Next task is APSI-040.

## M3 - Exit/Cancel Semantics And Closeout

- [x] APSI-040 [owner=codex] [deps=APSI-030] [scope=apps/android/app/src/main/java/dev/nako/android/ui/screens/player,apps/android/app/src/test,docs/workstreams/android-playback-session-integrity]
  Goal: Prove player exit cancels an active non-ended session or split the
  remaining active-session runtime blocker.
  Validation: focused Android tests, focused smoke evidence if available, and
  `git diff --check`.
  Review: no coroutine fire-and-forget behavior may be the only evidence for a
  cancellation claim.
  Evidence: `PlaybackExitEffectsTest`, smoke session readback evidence, and
  documented follow-on split in `EVIDENCE_AND_GATES.md`.
  Handoff: Close lane and open a realistic long-media/non-Direct runtime
  cancellation follow-on when ready.
