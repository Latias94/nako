# AAD-080 — Protocol/client crate boundary audit

Date: 2026-05-21
Owner: codex

## Deletion Test

Question: can `taru-addon-protocol` keep both wire contracts and the HTTP
caller helper without real Interface cost?

Result: no. Keeping the caller in the protocol crate made every Addon wire
contract consumer inherit `reqwest` and `async-trait`, even when the consumer
only needed manifests, envelopes, scopes, Protected Write payload DTOs, and
validation.

## Implementation

- Created `taru-addon-client` as a permissive `Apache-2.0` helper crate.
- Moved `AddonTransport`, `ReqwestAddonTransport`, `AddonHttpRequest`,
  `AddonHttpResponse`, `AddonClientError`, and `call_addon_resource` to
  `taru-addon-client`.
- Kept `taru-addon-protocol` focused on wire DTOs and validation.
- Removed `reqwest` and `async-trait` from `taru-addon-protocol` dependencies.
- Updated server reference-addon tests to import caller helpers from
  `taru-addon-client`.
- Updated ADR/docs to describe the protocol/client split.

## Validation

- `cargo check -p taru-addon-protocol -p taru-addon-client -p taru-reference-addon -p taru-server --tests`
- `cargo nextest run -p taru-addon-protocol -p taru-addon-client --no-fail-fast`
- `cargo nextest run -p taru-server reference_addon_registers_queries_and_handles_resource_call --no-fail-fast`

Final fmt/diff gates are recorded in `EVIDENCE_AND_GATES.md` when task closeout
is refreshed.
