# AAD-100 Journal — Closeout

Date: 2026-05-21

## Intent

Review, verify, and close the Addon Architecture Deepening lane without
leaving vague follow-up buckets.

## Review

No blocking workstream-compliance or code-quality findings remained:

- Addon Side Effect lifecycle now has a runtime boundary and common apply
  command shape.
- Idempotency distinguishes replay from same-key different-request conflict.
- Shipped Protected Write payloads are explicit Addon-facing protocol DTOs.
- Addon Manifest declarations and validation cover the next protocol concepts
  without pretending runtime breadth exists.
- Library File Write has a runtime seam with NFO Export as the first Adapter.
- Addon administration lives under `/admin/v1/addons` with Admin DTO shielding;
  root `/addons` management is intentionally removed.
- `taru-addon-protocol` is dependency-light and transport helpers live in the
  separate permissive `taru-addon-client` crate.
- Addon Side Effect request fingerprints are part of the clean SQLite and
  PostgreSQL base schemas, with no compatibility migration or nullable fallback.

No new follow-on was opened. Addon Manager, full Addon Task runtime, Addon
Event Subscription delivery, subtitle/sidecar breadth, Native Plugin ABI, and
Jellyfin Plugin Compatibility remain explicit non-goals.

## Validation

```text
cargo fmt --all -- --check
cargo check --workspace --tests
cargo nextest run -p taru-addon-protocol -p taru-addon-client --no-fail-fast
cargo nextest run -p taru-db addon --no-fail-fast
cargo nextest run -p taru-server addons --no-fail-fast
cargo nextest run -p taru-server addon_side_effect --no-fail-fast
cargo nextest run -p taru-server library_file_write --no-fail-fast
cargo nextest run -p taru-api --no-fail-fast
cargo nextest run --workspace --no-fail-fast
git diff --check
```

Results on 2026-05-21:

- Formatting passed.
- Workspace check passed.
- Addon protocol/client nextest passed: 12 passed.
- Addon DB nextest passed: 11 passed.
- Addon server nextest passed: 32 passed.
- Addon Side Effect nextest passed: 10 passed.
- Library File Write nextest passed: 3 passed.
- API nextest passed: 42 passed.
- Workspace nextest passed: 521 passed, 25 skipped.
- `git diff --check` passed with Git CRLF normalization warnings only.

PostgreSQL opt-in contracts were not run because `TARU_TEST_POSTGRES_URL` was
not set.
