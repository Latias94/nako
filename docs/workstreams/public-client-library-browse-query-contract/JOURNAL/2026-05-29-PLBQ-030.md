# PLBQ-030 Session Journal

Date: 2026-05-29
Task: PLBQ-030
Status: DONE

## Implemented

- Added Public Client `GET /libraries/{library_id}/items`.
- Added `LibraryItemsResponse`, browse sort/order/watch-state enums, and route inventory ownership in `nako-client-protocol`.
- Added OpenAPI schemas/path plus TypeScript and Kotlin SDK generation for `listLibraryItems`.
- Added Rust SDK `list_library_items` with typed page/sort/order/watch_state query helpers and facet pass-through.
- Added server-side query parsing for `limit`, `offset`, `sort`, `order`, repeated or comma-separated `facet`, and `watch_state`.
- Added Library Access hiding: inaccessible libraries return `404 not_found`.
- Added application filtering for `kind:` facets and principal-scoped watch-state filters.
- Added repository-level library item added-at facts so `date_added` sorts by library/source relationship time instead of item ID.

## Validation

- `cargo nextest run -p nako-server catalog --no-fail-fast`
- `cargo nextest run -p nako-client-protocol public_route_inventory --no-fail-fast`
- `cargo nextest run -p nako-api openapi --no-fail-fast`
- `cargo nextest run -p nako-api typescript_sdk --no-fail-fast`
- `cargo nextest run -p nako-api kotlin_sdk --no-fail-fast`
- `cargo nextest run -p nako-client client_builds_library_items_query_contract --no-fail-fast`
- `cargo nextest run -p nako-db sqlite_library_media_contract_preserves_library_scoped_source_identity --no-fail-fast`

## Notes

- PLBQ-030 implements `kind:<ClientMediaKind>` facets. Other frozen facet
  prefixes remain reserved and currently return `400 invalid_input`.
- PLBQ-040 should wire `web/` to this route and keep readiness messaging for
  unsupported facet prefixes.
