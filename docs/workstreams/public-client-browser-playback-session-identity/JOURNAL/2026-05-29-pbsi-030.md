# PBSI-030 Session Notes

Date: 2026-05-29
Task: PBSI-030
Status: DONE

## Changes

- Added required nullable `playback_session_id` to the browser playback ticket
  protocol/OpenAPI contract and regenerated TypeScript/Kotlin SDKs.
- Allocated durable playback sessions before returning non-subtitle browser
  ticket JSON.
- Bound opaque browser source tickets to the allocated playback session.
- Routed ticketed direct/remux/HLS media requests through the bound playback
  session instead of creating a second session.
- Kept subtitle browser tickets sessionless with `playback_session_id: null`.
- Allowed remux/HLS session media paths to lazily create and link their
  transcode artifacts back to the pre-created playback session.
- Enforced playback-session heartbeat ownership and effective play access.

## Validation

- `cargo nextest run -p nako-client-protocol public_browser_playback_ticket --no-fail-fast`
- `cargo nextest run -p nako-api browser_playback --no-fail-fast`
- `cargo nextest run -p nako-api sdk --no-fail-fast`
- `cargo nextest run -p nako-client playback_decision_ticket_and_session_cancel_paths_are_stable --no-fail-fast`
- `cargo nextest run -p nako-server browser_playback --no-fail-fast`
- `cargo nextest run -p nako-server playback_ticket --no-fail-fast`

## Next

Start PBSI-040 and wire `web/` heartbeat from
`BrowserPlaybackTicketResponse.playback_session_id`.
