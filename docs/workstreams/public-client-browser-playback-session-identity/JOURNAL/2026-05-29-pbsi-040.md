# PBSI-040 Session Notes

Date: 2026-05-29
Task: PBSI-040
Status: DONE

## Changes

- Added `playbackSessionId` to web Public Client playback plans from
  `BrowserPlaybackTicketResponse.playback_session_id`.
- Added Public Client data-source support for
  `POST /playback/sessions/{session_id}/heartbeat`.
- Routed media surface heartbeat calls through the public playback session id.
- Updated `VideoPlayer` to emit active, paused, ended, and failed heartbeat
  events with position and duration snapshots.
- Covered the data-source route and player behavior so heartbeat identity comes
  from `playback_session_id`, not ticketed media URLs or diagnostic headers.

## Validation

- `npm --prefix web run test -- src/test/data-source-contracts.test.ts src/test/video-player.test.tsx`
- `npm --prefix web run test`
- `npm --prefix web run check`
- `npm --prefix web run build:budget`
- `python -m json.tool docs/workstreams/public-client-browser-playback-session-identity/WORKSTREAM.json`
- `git diff --check -- docs/workstreams/public-client-browser-playback-session-identity web`

## Next

Start PBSI-050 closeout with final gates and follow-on notes.
