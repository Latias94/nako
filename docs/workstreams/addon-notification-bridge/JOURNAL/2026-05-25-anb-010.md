# ANB-010 — Scope And Evidence Freeze

Status: DONE
Date: 2026-05-25

## What Changed

- Opened `docs/workstreams/addon-notification-bridge/` as the first
  notification-specific follow-on after Addon Event Scheduler And Replay.
- Froze the boundary that Nako core owns event facts, scheduler delivery,
  grants, attempts, replay, filters, and redaction-safe diagnostics.
- Froze provider credentials, message templates, provider API calls, and
  provider fan-out as sidecar-owned behavior.
- Defined ANB-020 as a minimal official addon ACK proof before selecting
  Telegram, Discord, Home Assistant, email, or other provider breadth.

## Validation

```powershell
python -m json.tool docs/workstreams/addon-notification-bridge/WORKSTREAM.json > $null
git diff --check
```

Result: passed. `git diff --check` emitted only Windows CRLF conversion
warnings.

## Next

ANB-020 should implement the official addon-side manifest and
`library.scanned` ACK route in `F:\SourceCodes\Rust\nako-official-addons`.
