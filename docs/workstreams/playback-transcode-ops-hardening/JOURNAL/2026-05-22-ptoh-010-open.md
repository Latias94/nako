# 2026-05-22 — PTOH-010 Open

Opened `playback-transcode-ops-hardening` as the next mainline execution lane
under `post-rpd-product-hardening`.

## Context

`nfo-sidecar-promotion-apply` is complete. Nako now has accepted apply
boundaries for metadata provider breadth, NFO/link authority, managed import
staging, promotion apply, and NFO sidecar mutation. That means the next
highest operator risk is not another library mutation boundary; it is playback
and transcode supportability.

## Scope Frozen

In scope:

- readiness diagnostics;
- fallback reason categories;
- request/profile validation;
- failure taxonomy;
- bounded Admin support evidence.

Out of scope:

- downloader/watch-folder acquisition;
- metadata or NFO writes;
- adaptive HLS ladders;
- direct remote FFmpeg credentials;
- Addon runtime distribution;
- public client contract changes unless split.

## Next

Start PTOH-020 by defining the playback runtime readiness contract and proving
that it remains Admin-only and redaction-safe.
