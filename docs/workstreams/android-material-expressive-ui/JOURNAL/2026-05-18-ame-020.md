# AME-020 Journal

Date: 2026-05-18

## Summary

Implemented the first Android Material Expressive UI foundation slice:

- added optional dynamic color support to `NakoAndroidTheme`;
- introduced artwork accent generation with deterministic fallback;
- extracted shared UI surfaces and motion tokens into `ui/components`;
- added an adaptive phone bottom navigation / tablet rail shell;
- routed `NakoBrowseShell` through the new shell;
- kept the existing Public Client API and Media3 boundaries intact;
- added JVM coverage for artwork accent determinism.

## Validation

- `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`
- `apps\android\gradlew.bat -p apps\android :app:assembleDebug --no-daemon`

## Notes

- V2 is the initial target.
- V3 irregular geometry remains deferred.
- The next slice is `AME-030` for Home and browse surfaces.
