# AME-060 Journal

Date: 2026-05-18

## Summary

Closed the Android Material Expressive UI workstream after verifying the V2 UI
rewrite.

- Confirmed the lane satisfies the V2 scope: Home, Libraries, Browse Facet
  Result, Media Item Detail, Source / Version Picker, Player, Settings Home,
  and Server Profile now share the Material 3 design vocabulary.
- Confirmed the lane preserved Public Client API, token-redaction,
  active-server, Media3 playback launch, and playback-session boundaries.
- Recorded deferred work instead of hiding unsupported behavior in Android-only
  UI.

## Validation

- `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`
  passed.
- `apps\android\gradlew.bat -p apps\android :app:assembleDebug --no-daemon`
  passed.
- `cargo fmt --all -- --check` passed.
- `cargo nextest run --workspace --no-fail-fast` passed with 364 tests passed
  and 0 skipped.
- `git diff --check` passed.

## Follow-Ons

- V3 irregular/freeform geometry exploration.
- Authoritative User Playback State and cross-device Continue Watching through
  Public Client API/server support.
- Richer source facts, tracks/subtitles, chapters, and source diagnostics
  through explicit Public Client API support.
- Downloads/offline playback, external player handoff, picture-in-picture, and
  advanced player gestures.
- Compose clipboard API migration after the replacement is clear for the app's
  Compose baseline.
