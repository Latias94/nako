# AME-040 Journal

Date: 2026-05-18

## Summary

Implemented the V2 Media Item Detail and Source / Version Picker slice:

- Replaced the old browse-package detail screen with
  `ui/screens/detail/MediaItemDetailRoute`.
- Added an artwork-led playback decision hero with Play/Resume hierarchy and
  explicit device-local resume wording.
- Added `ui/screens/sourcepicker/SourcePickerScreen` for selected-source state,
  client-safe source facts, and playback-mode consequences.
- Preserved Public Client API playback decision requests and Media3 launch
  construction in `TaruBrowseShell`.
- Added JVM tests for source-picker display models and locator-safe visible
  source facts.

## Validation

- `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`
- `apps\android\gradlew.bat -p apps\android :app:assembleDebug --no-daemon`
- `git diff --check`

## Notes

- No server-local paths, token values, or HLS playlist parsing were introduced.
- Device-local playback position is only used as local resume wording and
  launch input; it is not presented as authoritative User Playback State.
- The next slice is `AME-050` for Player and Settings V2.
