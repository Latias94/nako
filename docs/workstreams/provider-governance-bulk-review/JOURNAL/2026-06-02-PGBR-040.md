# PGBR-040 Journal

Date: 2026-06-02
Status: DONE

## Scope

Implemented Web Admin batch governance for the global Metadata Candidate
Review queue.

Changed areas:

- `web/src/api/admin/client.ts`
- `web/src/api/admin/read-models-data-source.ts`
- `web/src/api/admin/mutations-data-source.ts`
- `web/src/features/admin/admin-metadata-candidate-review.tsx`
- `web/src/test/route-state-contracts.test.tsx`
- `web/scripts/check-bundle-budget.mjs`
- `docs/workstreams/provider-governance-bulk-review`

## Implementation Notes

- Added Web client methods for batch plan and batch apply Admin routes.
- Added selected-row state and controls on the global Candidate Review queue.
- Added read-only batch plan inspection before confirmation.
- Added explicit live confirmation that sends review ID, item ID,
  expected update time, and a generated idempotency key.
- Rendered partial results through a narrow Web read model that only carries
  status, reasons, and Provider Mapping ID for batch rows.
- Kept fixture batch plans fallback-only and non-confirmable.
- Raised total JS gzip budget by 1 KiB after slimming the UI; route budgets
  remain below their limits.

## Validation

- `npm --prefix web run check` passed.
- `npm --prefix web run test` passed: 10 files, 121 tests.
- `npm --prefix web run build:budget` passed: total JS `344.77/345 KiB`.
- Browser smoke passed with mocked Admin API:
  queue selection -> batch plan -> confirmation -> result, with no raw
  provider response, raw idempotency key, or bearer token in page text.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Follow-On

`PGBR-050` should close the lane or split follow-ons for durable job execution,
related hierarchy application, provider endpoint depth, Public Client API
exposure, and broader provider governance.
