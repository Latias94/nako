# PGBR-020 Journal

Date: 2026-06-02
Task: PGBR-020
Status: DONE

## Summary

Added the read-only Admin API batch Candidate Review application plan for
selected review IDs. The route is bounded, deterministic for duplicate IDs,
redaction-safe, and delegates per-row classification to the existing
single-review application planner.

## Implementation

- Added `AdminMetadataCandidateReviewBatchPlanRequest`,
  `AdminMetadataCandidateReviewBatchPlanResponse`, and summary DTOs.
- Added `POST /admin/v1/metadata/candidate-reviews/batch-application-plan`.
- Added service validation for non-empty, unique selections capped at 50 review
  IDs.
- Synchronized generated Admin TypeScript contracts in `apps/admin-web` and
  `web`.

## Validation

- RED: focused server system test failed before the batch DTO/service/route
  existed.
- GREEN: `cargo test -p nako-server admin_v1_metadata_candidate_review_batch_plan_is_bounded_redacted_and_read_only -- --nocapture`
  passed.
- `cargo test -p nako-api admin_contract -- --nocapture` passed.
- `cargo test -p nako-server metadata_candidate_review -- --nocapture` passed.
- `cargo test -p nako-metadata candidate_review_application -- --nocapture`
  passed.
- `cargo fmt --all -- --check` passed.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Notes

`cargo nextest` is unavailable in this checkout, so focused `cargo test` gates
were used. No batch mutation, Web UI, Public Client API, related hierarchy
application, provider endpoint depth, or durable job execution was added.
