# 2026-05-20

- Opened as the follow-on to the closed Android structured validation reports
  lane.
- Existing state:
  - `Smoke-Regression.ps1` writes Markdown and JSON reports.
  - `Validate-AndroidLocal.ps1` writes Markdown and JSON reports and links
    delegated smoke reports.
- Scope for this lane is additive JUnit XML report generation for CI test
  surfaces.
- Out of scope:
  - CI upload/artifact retention.
  - Golden screenshot diffing.
  - Python rewrite of the validation scripts.
- First executable task is AJVR-010: freeze the JUnit report contract in docs
  before changing scripts.
- Completed AJVR-010:
  - `DESIGN.md` now defines `report.junit.xml`.
  - Smoke regression suite: `taru.android.smoke-regression`.
  - Local validation suite: `taru.android.local-validation`.
  - Testcase naming, pass/fail/skipped mapping, allowed properties, and
    token-safety constraints are explicit.
- Next task is AJVR-020: add smoke regression JUnit XML output.
- Completed AJVR-020:
  - `Smoke-Regression.ps1` now writes `report.junit.xml`.
  - The script prints `JUnit report: <path>` and includes `report_junit` in
    `report.json`.
  - Focused smoke validation passed:
    `pwsh -NoProfile -File apps\android\scripts\Smoke-Regression.ps1 -States empty-setup -SkipBuild -RetriesPerState 0 -OutputRoot apps\android\build\smoke-regression-ajvr`.
  - Generated JUnit XML parsed successfully:
    `apps/android/build/smoke-regression-ajvr/20260520-201331/report.junit.xml`.
- Next task is AJVR-030: add local validation JUnit XML output.
