# AATC-050 — Boundary And Closeout

Date: 2026-05-20
Status: complete

## Work Completed

- Added a focused `taru-api` guard proving generated Admin API read-model route
  constants stay out of `taru-client-protocol` public route inventory.
- Documented the app-local Admin API contract generation command in
  `DESIGN.md`, `docs/api/HTTP_API.md`, and `apps/admin-web/README.md`.
- Documented the Public Client SDK separation check:
  `npm run generate --prefix sdk/typescript`,
  `npm run check --prefix sdk/typescript`, and
  `git diff --name-only -- crates/taru-client-protocol sdk/typescript`.
- Closed the workstream and split remaining product work into follow-on
  directions rather than keeping hidden active tasks.

## Validation

```bash
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo check -p taru-api --examples
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo nextest run -p taru-api admin_contract --no-fail-fast -j 2
$env:TMP='F:\Temp'; $env:TEMP='F:\Temp'; cargo nextest run -p taru-api typescript --no-fail-fast -j 2
cd apps/admin-web && npm run check
cd apps/admin-web && npm run test
cd apps/admin-web && npm run build
npm run generate --prefix sdk/typescript
npm run check --prefix sdk/typescript
git diff --name-only -- crates/taru-client-protocol sdk/typescript
cargo fmt --all -- --check
git diff --check
```

Results:

- `taru-api` example check passed.
- Admin Contract nextest passed: 5 passed, 37 skipped.
- TypeScript SDK nextest passed: 6 passed, 36 skipped.
- Admin-web check passed.
- Admin-web tests passed: 9 passed across 3 files.
- Admin-web build passed.
- Public TypeScript SDK generation and check passed.
- Public SDK/protocol diff check produced no output.
- Format and diff checks passed.

## Follow-ons

- Admin npm package only if a second Admin API consumer appears.
- Jobs filters and job detail entry point.
- Catalog Governance filters and item review detail.
- Playback sessions filters and session detail.
- Settings diagnostics layout polish before editable settings.
