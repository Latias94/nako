# Link Apply And Import Promotion LAIP-030 — 2026-05-21

## Summary

Implemented app-service promotion acceptance/replay without storage mutation or
Media Source writes.

## Shipped

- Added `AcceptManagedImportPromotionRequest`.
- Added `ManagedImportAppService::accept_promotion`.
- Added redacted `ManagedImportPromotionAcceptanceDiagnostic`.
- Records explicit operator principal, idempotency key, selected operation,
  accepted plan snapshot, accepted warning snapshot, and non-mutating outcome.
- Replays matching idempotency keys.
- Rejects mismatched idempotency-key reuse.
- Rejects blocked plans before durable apply creation.
- Keeps move deferred.
- Allows copy acceptance when the only blocker is missing link-planning
  registry, because copy mutation is a future VFS apply path and not a link
  dry-run.

## Validation

- `cargo nextest run -p nako-server managed_import --no-fail-fast`

Eight Managed Import app tests passed.

## Follow-On

LAIP-040 should add VFS-mediated copy/hardlink/symlink apply primitives. Server
code must continue to avoid direct OS path mutation.
