# Link Apply And Import Promotion LAIP-020 — 2026-05-21

## Summary

Implemented the durable promotion acceptance/audit domain before any storage
mutation behavior.

## Shipped

- Added `ManagedImportPromotionApplyId`.
- Added `ManagedImportPromotionApplyState` lifecycle vocabulary.
- Added durable apply records with:
  - artifact and target library IDs;
  - operator principal;
  - idempotency key;
  - selected operation;
  - source artifact URI and destination locator;
  - accepted plan/warning JSON snapshots;
  - outcome and redacted safe error fields.
- Extended `ManagedImportRepository` with apply upsert/get/idempotency lookup,
  artifact-scoped listing, and state update.
- Added SQLite and PostgreSQL migrations.
- Added SQLite/PostgreSQL repository adapters and facade forwarding.
- Added a backend-neutral contract test for acceptance/audit round-trip and
  cleanup-pending state updates.

## Validation

- `cargo fmt --all -- --check`
- `cargo nextest run -p taru-db managed_import --no-fail-fast`
- `cargo nextest run -p taru-db promotion_apply --no-fail-fast`
- `python -m json.tool docs/workstreams/managed-import-staging/WORKSTREAM.json`
- `python -m json.tool docs/workstreams/link-apply-and-import-promotion/WORKSTREAM.json`
- `python -m json.tool docs/workstreams/post-rpd-product-hardening/WORKSTREAM.json`
- `git diff --check`

All passed. `git diff --check` emitted only repository CRLF conversion
warnings.

## Follow-On

LAIP-030 should add app-service acceptance/replay. It must still avoid storage
mutation and Media Source writes.
