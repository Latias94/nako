# Playback Media Maturity First Slices

Durable fearless refactor lane for deepening Nako playback after the
source-aware transcode runtime and rendition-planning boundaries landed.

This lane owns the first closable slices for:

- public client capability profile input;
- adaptive HLS and fMP4 rendition planning vocabulary;
- subtitle, HDR, audio-channel, bitrate, and resolution transcode maturity.

