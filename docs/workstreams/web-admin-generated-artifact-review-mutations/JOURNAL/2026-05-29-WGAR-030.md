# 2026-05-29 - WGAR-030

Added the guarded review route and queue entry points:

- `/admin/automation/generated-artifacts/review`
- route-owned `artifact_id` and `decision` state
- review-plan display for decision/action/reasons/target/payload/readiness
  and boundary flags
- explicit confirmation flow
- fixture-disabled mutation state
- live mutation result and idempotent replay display
- proposal/review-plan query invalidation after success
- live connections cannot confirm when review-plan falls back to fixture data
- sticky queue review action column for desktop and mobile visibility

Validation:

```bash
npm --prefix web run test -- src/test/route-contracts.test.tsx src/test/route-state-contracts.test.tsx
npm --prefix web run check
```

Result: passed.
