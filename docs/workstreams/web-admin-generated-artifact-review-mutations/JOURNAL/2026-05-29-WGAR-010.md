# 2026-05-29 - WGAR-010

Opened `web-admin-generated-artifact-review-mutations` as the follow-on from
the closed read-only Generated Artifacts automation lane.

Important correction: the Rust server registers Generated Artifact
`review-plan` as `POST`, not `GET`. The frontend implementation must send
`{ decision }` in the request body and should test that method/body shape.

Next task: `WGAR-020`.
