# ACQ-040 Journal

Date: 2026-05-18

## Summary

Added the first repeatable emulator surface coverage:

- Added `-FixtureState` with `current-state`, `empty-setup`, and
  `profile-missing-token`.
- Kept `-ResetAppData` as a compatibility shortcut for `empty-setup`.
- Captured named screenshots, UI hierarchy dumps, and criteria files.
- Added setup coverage for `empty-setup`.
- Added Home, Settings, and Server Profile shell coverage for
  `profile-missing-token`.
- Seeded only Android-local Server Profile state for `profile-missing-token`;
  no token value or fake media data is seeded.
- Added retry handling around `uiautomator dump` because the emulator can
  briefly report no root node during launch or route changes.

## Validation

- `pwsh -NoProfile -File apps\android\scripts\Smoke-Emulator.ps1 -FixtureState profile-missing-token`
  passed on `emulator-5554`.
- Evidence was written under
  `apps/android/build/smoke/20260518-214533-profile-missing-token-emulator-5554/`.
- `home.criteria.txt`, `settings.criteria.txt`, and
  `server-profile.criteria.txt` all reported `Result: PASS`.
- `pwsh -NoProfile -File apps\android\scripts\Smoke-Emulator.ps1 -FixtureState empty-setup`
  passed on `emulator-5554`.
- Evidence was written under
  `apps/android/build/smoke/20260518-214452-empty-setup-emulator-5554/`.
- `setup.criteria.txt` reported `Result: PASS`.
- Evidence reports and criteria files were checked for token-reference values,
  bearer tokens, localhost, and `10.0.2.2`; none were present.

## Notes

- `profile-missing-token` intentionally exercises the authenticated shell in
  a missing-token state. This proves navigation and settings surfaces without
  depending on a live server.
- Home media content, detail, source picker, and player evidence remain
  server-backed fixture follow-ons.
