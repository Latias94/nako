# ACQ-020 Journal

Date: 2026-05-18

## Summary

Implemented the first local Android smoke harness slice:

- Added `apps/android/scripts/Smoke-Emulator.ps1`.
- Updated `apps/android/README.md` with local smoke usage, output path, and
  emulator prerequisites.
- The script resolves `adb`, starts the adb server, selects a connected device
  or accepts `-Serial`, builds `:app:assembleDebug` by default, installs the
  debug APK, launches `dev.taru.android/.MainActivity`, captures `launch.png`,
  and writes a `report.md` under `apps/android/build/smoke/`.
- The smoke command was verified on `emulator-5554` and produced launch/smoke
  evidence under `apps/android/build/smoke/20260518-204538-emulator-5554/`.

## Validation

- `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`
  passed.
- `apps\android\gradlew.bat -p apps\android :app:assembleDebug --no-daemon`
  passed.
- `pwsh -NoProfile -File apps\android\scripts\Smoke-Emulator.ps1` passed on
  `emulator-5554`.
