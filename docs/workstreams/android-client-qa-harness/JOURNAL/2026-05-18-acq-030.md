# ACQ-030 Journal

Date: 2026-05-18

## Summary

Defined the first repeatable Android smoke fixture strategy:

- Added `apps/android/SMOKE_FIXTURES.md`.
- Added `-ResetAppData` to `apps/android/scripts/Smoke-Emulator.ps1`.
- Smoke evidence output now includes the state mode in the directory name and
  report.
- Documented `current-state` as a fast non-deterministic launch check and
  `empty-setup` as the deterministic setup-state fixture.
- Deferred profile/media/player fixtures until they can use Public Client API
  backed state instead of Android-only fake data.

## Validation

- `pwsh -NoProfile -File apps\android\scripts\Smoke-Emulator.ps1 -ResetAppData`
  passed on `emulator-5554`.
- Evidence was written under
  `apps/android/build/smoke/20260518-205953-empty-setup-emulator-5554/`.
- `nako_server_profiles.xml` was absent after reset and launch.
- `nako_access_tokens.xml` contained AndroidX Security keysets only; no app
  token entry was present.

## Notes

- `pm clear dev.nako.android` runs after install and before launch.
- The script force-stops the app before launch for both state modes.
- Generated screenshots and reports remain untracked.
