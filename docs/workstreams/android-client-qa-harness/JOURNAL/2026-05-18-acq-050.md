# ACQ-050 Journal

Date: 2026-05-18

## Summary

Verified and closed the Android Client QA Harness lane.

The shipped harness now provides:

- documented local Android JVM/build/smoke commands;
- deterministic `empty-setup` setup-surface evidence;
- deterministic `profile-missing-token` Home, Settings, and Server Profile
  shell evidence;
- named screenshots, UI hierarchy dumps, and pass/fail criteria files;
- generated evidence under ignored `apps/android/build/smoke/` paths.

## Validation

- `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`
  passed.
- `apps\android\gradlew.bat -p apps\android :app:assembleDebug --no-daemon`
  passed.
- `pwsh -NoProfile -File apps\android\scripts\Smoke-Emulator.ps1 -FixtureState empty-setup`
  passed on `emulator-5554`.
- `pwsh -NoProfile -File apps\android\scripts\Smoke-Emulator.ps1 -FixtureState profile-missing-token`
  passed on `emulator-5554`.
- `git diff --check` passed.

Evidence paths:

- `apps/android/build/smoke/20260518-215542-empty-setup-emulator-5554/`
- `apps/android/build/smoke/20260518-215751-profile-missing-token-emulator-5554/`

## Follow-Ons

- CI device execution.
- Golden visual diffing and reference screenshot policy.
- Server-backed demo fixtures for media content, detail, source picker, and
  player coverage.
- Instrumentation test migration once routes stabilize.
- Optional ADB daemon reconnect hardening if local smoke runs continue to hit
  daemon restarts.
