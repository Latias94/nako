# HSMA-020 Journal - Duplicated Audio Characterization

Date: 2026-05-29
Task: HSMA-020

## Summary

HSMA-020 added focused `nako-transcode` tests that prove the current duplicated
selected-audio output shape before changing behavior.

## Findings

- Single-variant HLS with generated audio sidecars maps selected audio into the
  primary video output and again as a sidecar.
- Adaptive HLS with generated audio sidecars maps selected audio once per video
  rendition and again as a sidecar.
- The behavior change belongs in FFmpeg HLS command planning, with server HLS
  facts used only to decide when audio sidecars are present.

## Validation

- `cargo nextest run -p nako-transcode hls --no-fail-fast` (42 passed, 38
  skipped)
- `cargo nextest run -p nako-server hls --no-fail-fast` (53 passed, 422
  skipped)

## Next

HSMA-030 should convert the characterization tests to the desired output shape:
sidecar-capable main video outputs should not carry selected audio, while
single-audio and no-sidecar outputs preserve existing muxed-audio behavior.
