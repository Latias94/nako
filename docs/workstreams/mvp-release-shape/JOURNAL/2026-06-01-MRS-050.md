# MRS-050 Journal - Campaign Integration And RC Validation

Date: 2026-06-01

## Summary

Campaign A (`PTJCH-220`) and Campaign B (`web-mvp-live-smoke`) are integrated
on `main`. Release-candidate validation has run from the documented MVP ladder.

## Evidence

- Gate 0 planner/docs preflight passed:
  `WORKSTREAM.json` JSON validation, workstream inventory, docs whitespace
  check, and `scripts/release-gate.ps1 -Mode docs`.
- Gate 1 core release preflight passed:
  `scripts/release-gate.ps1 -Mode fast`.
- Gate 2 server MVP journey passed:
  `cargo nextest run -p nako-server self_host_smoke scan_library metadata storage addons playback hls user_playback --no-fail-fast`
  passed 341/341 with 169 skipped.
- Gate 3 Web/Public Client validation passed: `npm --prefix web run test`
  passed 98/98 tests, `npm --prefix web run check` passed, and
  `npm --prefix web run build:budget` passed.
- Gate 4 playback runtime evidence is covered by the Gate 2 `playback`/`hls`
  filters plus the post-merge PTJCH seek gate.
- Gate 5 package and container shape passed:
  `scripts/package-release.ps1 -WhatIf` and
  `scripts/release-gate.ps1 -Mode container`.
- Gate 6 official addon alpha smoke is skipped by MVP scope because this
  candidate does not claim an official Addon Sidecar proof.
- Gate 7 PostgreSQL compatibility passed:
  `scripts/release-gate.ps1 -Mode postgres` started a temporary local
  PostgreSQL harness and passed 6/6 managed-artwork contract tests.

## Fixes During Validation

- `d1c8550d` synchronized Admin Web release mock fixtures with generated
  contract DTOs after the first Gate 1 run failed.
- `94385f98` aligned the official external acquisition runner catalog resolve
  test with the optional `transmission_password` Secret Reference declaration
  after the first Gate 2 run failed.
- The playback permit preflight failure in the first Gate 2 run passed on
  focused rerun and on the final combined Gate 2 rerun.

## Next

MRS-050 is eligible for closeout after planner/user confirmation. Keep
Campaign C optional unless one-command release proof becomes required, and keep
Campaign D conditional on an official Addon Sidecar release claim.
