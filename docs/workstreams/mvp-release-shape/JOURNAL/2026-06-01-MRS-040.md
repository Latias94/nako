# MRS-040 Active Queue Alignment

Date: 2026-06-01
Status: DONE_WITH_CONCERNS

## Goal

Align the active PTJCH, GAMA, and CSAPA tails with the MVP release cut without
mutating their active task ledgers.

## Result

- `PTJCH-220` remains the current P0 playback blocker. It owns Playback Runtime
  session/admission/reuse/supersede/cancel/failure diagnostics and is required
  before MVP closeout unless the risk is explicitly accepted.
- `GAMA-060` is conditional/P1. Backend Generated Artifact metadata apply is
  stable through `GAMA-050`; the remaining Web Admin apply workflow is not
  required for browser/web MVP unless product scope changes.
- `CSAPA-050` is deferred/P1. Desktop playback strategy is post-MVP while
  browser/web remains the first accepted client path.
- Web/Public Client live smoke and an operations-release gate wrapper remain
  possible `MRS-050` splits.

## Parallel State

The `PTJCH-220` worker has been started in the playback worktree. The MVP
planner lane should not edit playback task ledgers while that worker is active;
review and merge should go through `integrate-lane-results`.
