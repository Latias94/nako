# GAMA-050 - Admin Metadata Apply Route

Date: 2026-05-30
Status: DONE

## Scope

Implemented only the backend Admin metadata apply route and generated contract
sync. Web Admin workflow work remains `GAMA-060`.

## Changes

- Added `AdminGeneratedArtifactMetadataApplyRequest` and
  `AdminGeneratedArtifactMetadataApplyResponse` in `nako-api`.
- Exposed
  `POST /admin/v1/automation/generated-artifacts/{artifact_id}/metadata-apply`
  in `nako-server`.
- Routed the HTTP body `idempotency_key` into the existing durable
  `GeneratedArtifactMetadataApplyRequest` app contract.
- Added HTTP tests for auth, method/path/body, redacted success response,
  idempotent replay, invalid idempotency-key mapping, and missing artifact
  mapping.
- Updated the Admin TypeScript contract generator and refreshed generated
  contracts in `apps/admin-web` and `web`.

## Evidence

- `cargo nextest run -p nako-api generated_artifact_metadata_apply --no-fail-fast`
- `cargo nextest run -p nako-api admin_contract --no-fail-fast`
- `cargo nextest run -p nako-server admin_generated_artifact_metadata_apply --no-fail-fast`
- `cargo fmt --all -- --check`
- `python -m json.tool docs/workstreams/generated-artifact-metadata-authority-apply/WORKSTREAM.json`
- `git diff --check -- docs/workstreams/generated-artifact-metadata-authority-apply docs/architecture/WORKSTREAM_LINKS.md docs/workstreams/README.md`
- `git diff --check`

## Notes

- Response redaction assertions cover raw prompt and artifact field names,
  Source Locator/path, generated overview text, explanation text, secret terms,
  and private source fingerprint.
- No schema or persistence behavior changed in this task; `GAMA-040` remains
  the persistence authority.
