# CRFBA-030 Journal - 2026-05-24

## Summary

Extracted the library source-observation commit path into
`crates/nako-library/src/ingestion/source_commit.rs`.

## What Changed

- `ingestion.rs` now delegates source-observation planning to the new internal
  module.
- The new module owns the local-inference planning, provisional ancestor reuse,
  and search-projection assembly.
- Tests were rewritten to use a tiny fake repository instead of the full
  database for the new seam.

## Validation

- `cargo nextest run -p nako-library source_observation_plan --no-fail-fast`
- `cargo nextest run -p nako-library local_inference::tests::local_inference_engine_maps_name_parser_output_at_library_boundary --no-fail-fast`
- `cargo nextest run -p nako-library local_inference::tests::local_inference_engine_plans_episode_hierarchy_from_file_name --no-fail-fast`
- `cargo nextest run -p nako-library local_inference::tests::local_inference_engine_keeps_unknown_source_flat_with_evidence --no-fail-fast`

## Blocker

- The broader `cargo nextest run -p nako-library local_inference --no-fail-fast`
  filter is still blocked by an unrelated database migration conflict from the
  addon credentials work.

## Next Step

- Continue the same lane with the next narrower boundary, or move to the next
  task once this slice is reviewed.
