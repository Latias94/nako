# CRFBA-040 Journal

Date: 2026-05-24
Owner: codex

## Summary

Deepened the managed-artwork candidate acceptance seam in
`crates/nako-server/src/app/artwork.rs`.

## Selection Follow-Up

Extended the same file with a dedicated artwork selection workflow store so
publish, select, and unpublish no longer call the broad store directly.

## Ingest Follow-Up

Extended the same file again with a dedicated artwork ingest workflow store so
claim, requeue, commit, and fail no longer call the broad store directly.

## Changes

- Added `ArtworkAcceptanceWorkflowStore` as a narrow workflow port for artwork
  candidate lookup, media lookup, library-item-state validation, and
  acceptance commit.
- Added `ArtworkSelectionWorkflowStore` as a narrow workflow port for artwork
  publish/select/unpublish operations.
- Added `ArtworkIngestWorkflowStore` as a narrow workflow port for artwork
  claim/requeue/commit/fail operations.
- Kept the rest of `ManagedArtworkAppService` on the existing broad store for
  gallery, artifact lifecycle, and cleanup work.
- Updated CRFBA coordination docs to treat CRFBA-040 as the active task.

## Validation

- `cargo fmt --all -- --check`
- `cargo nextest run -p nako-server admin_accept_artwork_candidate_queues_managed_ingest_without_public_artwork_or_url_echo --no-fail-fast`
- `cargo nextest run -p nako-server admin_managed_artwork_gallery_lists_candidates_artifacts_and_selection_without_locator_leaks --no-fail-fast`
- `cargo nextest run -p nako-server admin_selected_artwork_unpublish_hides_public_image_without_deleting_artifact --no-fail-fast`
- `cargo nextest run -p nako-server admin_process_next_managed_artwork_ingest --no-fail-fast`
- `cargo nextest run -p nako-server admin_managed_artwork_ingest_requeue --no-fail-fast`

## Follow-up

Review whether the remaining managed-artwork operations should share the same
acceptance authority slice or be split into follow-on tasks.
