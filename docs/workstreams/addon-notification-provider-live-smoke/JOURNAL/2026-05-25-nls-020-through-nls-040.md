# 2026-05-25 — NLS-020 Through NLS-040

Status: DONE

Implemented opt-in live provider smoke for `nako-notification-bridge`.

Key decisions:

- `smoke.live.ps1` skips unless `NAKO_NOTIFICATION_BRIDGE_LIVE_SMOKE=1`.
- The script expects a sidecar that is already configured with exactly one
  provider send path.
- The script validates provider-send ACK shape and redaction sentinel absence.
- Default CI must not require live provider secrets.

Validation:

```powershell
$errors = $null; [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw -Path 'addons\notification-bridge\smoke.live.ps1'), [ref]$errors) | Out-Null; if ($errors) { $errors; exit 1 }
Remove-Item Env:NAKO_NOTIFICATION_BRIDGE_LIVE_SMOKE -ErrorAction SilentlyContinue; pwsh -File addons/notification-bridge/smoke.live.ps1
```

Result: passed. Default run skipped without secrets.

Handoff: Lane closed. Enabled live-provider execution remains local/operator
owned.
