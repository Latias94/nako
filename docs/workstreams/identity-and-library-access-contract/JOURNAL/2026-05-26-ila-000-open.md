# ILA-000 Open

Date: 2026-05-26

Opened `identity-and-library-access-contract`.

Context:

- Nako currently has Single-Admin Mode and a bearer-token inbound auth
  boundary.
- User Playback State uses a stable principal, but there is no real user table.
- The user accepted fearless refactoring and explicitly allowed database
  migration consolidation because there are no current users.

Initial execution direction:

- First task is ILA-010, schema baseline and domain contract.
- SQLite and PostgreSQL migrations may be collapsed into backend-owned
  baselines if compatibility remains irrelevant.
- Repository contract tests must prove behavior before old migration files are
  removed.

No code was changed in this task.
