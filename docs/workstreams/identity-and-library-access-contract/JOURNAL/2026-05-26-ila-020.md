# ILA-020 Repository And Principal Resolution

Date: 2026-05-26
Status: DONE

## Summary

- Implemented `IdentityAccessRepository` for SQLite and PostgreSQL.
- Added backend-neutral contract coverage for User persistence, Role
  replacement, Library Access policy upsert/delete/list, and effective access.
- Added deterministic bootstrap administrator helpers in `nako-core`.
- Startup now creates or verifies the `local-admin` bootstrap administrator and
  preserves the existing bearer-token boundary.
- Inbound auth now inserts both the legacy `UserPrincipalId` and an
  `AuthenticatedPrincipal` for future user-aware handlers.
- Updated Admin access summary readiness and copy to reflect active backend
  identity storage while keeping mutation UI absent until Admin API edit
  contracts exist.

## Gates

- `cargo nextest run -p nako-core identity --no-fail-fast`
- `cargo nextest run -p nako-db user --no-fail-fast`
- `cargo nextest run -p nako-server auth --no-fail-fast`
- `cargo nextest run -p nako-server app_startup_creates_deterministic_bootstrap_admin_user admin_v1_access_summary --no-fail-fast`
- `cargo fmt --all -- --check`
- `python -m json.tool docs\workstreams\identity-and-library-access-contract\WORKSTREAM.json > $null`
- `npm run test -- App.test.tsx -t "Users & Access"`
- `git diff --check`

All listed gates passed.

`cargo clippy -p nako-core -p nako-db -p nako-server --all-targets -- -D warnings`
was also attempted. ILA-020-specific clippy findings were fixed, but the gate
remains blocked by existing unrelated warnings across other workspace crates and
pre-existing modules.

## Follow-On

ILA-030 should add explicit Admin API user, Role, Library Access, and bootstrap
readiness contracts before Admin Web exposes edit controls.
