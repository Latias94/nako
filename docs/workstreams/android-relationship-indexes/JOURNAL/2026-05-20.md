# 2026-05-20

- Opened from APICI-060 after Android API Contract Integration closed.
- Completed ARI-010:
  - Genres Index accepted as the first implementation slice.
  - Tags Index accepted as a second reuse slice after Genres proves the route
    and screen shape.
  - Top-level People Index deferred; Person Detail remains the primary People
    path from Cast & Crew.
  - Relationship indexes should open as nested routes from Home, not as new
    bottom navigation destinations.
- Next task is ARI-020: add typed Android client coverage for
  `GET /genres?limit=&offset=`.
- Completed ARI-020:
  - Added `GenreListResponse` and `TaruBrowseClient.listGenres`.
  - Added focused client tests for request URL construction, bearer auth,
    response decoding, safe diagnostics, and unsupported API version handling.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.taru.android.browse.TaruBrowseClientTest --no-daemon`.
- Next task is ARI-030: productize Genre Index route state before building the
  screen.
- Completed ARI-030:
  - Added `RelationshipIndexFamily.Genres`, `TaruRoute.RelationshipIndex`, and
    `RelationshipIndexUiState`.
  - Added state save/restore, route loading, retry, back behavior, and stale
    response protection.
  - `ClientBrowseDataSource.loadRelationshipIndex` maps `GET /genres` rows to
    existing Genre related Media Items route targets.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests "dev.taru.android.ui.browse.*" --no-daemon`.
- Next task is ARI-040: build the Material Expressive Genre Index screen and
  Home entry point.
