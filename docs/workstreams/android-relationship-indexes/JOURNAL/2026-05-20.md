# 2026-05-20

- Opened from APICI-060 after Android API Contract Integration closed.
- Completed ARI-010:
  - Genres Index accepted as the first implementation slice.
  - Tags Index accepted as a second reuse slice after Genres proves the route
    and screen shape.
  - Top-level People Index deferred; Person Detail remains the primary People
    path from Cast & Crew.
  - Relationship indexes should open as nested routes from Home, not as new
    bottom navigation destinations.
- Next task is ARI-020: add typed Android client coverage for
  `GET /genres?limit=&offset=`.
- Completed ARI-020:
  - Added `GenreListResponse` and `NakoBrowseClient.listGenres`.
  - Added focused client tests for request URL construction, bearer auth,
    response decoding, safe diagnostics, and unsupported API version handling.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests dev.nako.android.browse.NakoBrowseClientTest --no-daemon`.
- Next task is ARI-030: productize Genre Index route state before building the
  screen.
- Completed ARI-030:
  - Added `RelationshipIndexFamily.Genres`, `NakoRoute.RelationshipIndex`, and
    `RelationshipIndexUiState`.
  - Added state save/restore, route loading, retry, back behavior, and stale
    response protection.
  - `ClientBrowseDataSource.loadRelationshipIndex` maps `GET /genres` rows to
    existing Genre related Media Items route targets.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --tests "dev.nako.android.ui.browse.*" --no-daemon`.
- Next task is ARI-040: build the Material Expressive Genre Index screen and
  Home entry point.
- Completed ARI-040:
  - Added `RelationshipIndexRouteContent` with artwork-led header, loading,
    failure, empty, and content states.
  - Added the Home Genres anchor as a nested route entry point.
  - Added focused presentation coverage for counts and stable Genre targets.
  - Validation passed:
    `apps\android\gradlew.bat -p apps\android :app:testDebugUnitTest --no-daemon`.
- Next task is ARI-050: decide smoke proof versus non-smoke closeout, then
  close or split Tags Index follow-on.
- Completed ARI-050:
  - Extended `profile-with-media` smoke with a Home -> Genres -> Mystery ->
    Related Media Items proof.
  - Validation passed:
    `pwsh -NoProfile -File apps\android\scripts\Smoke-Regression.ps1 -States profile-with-media -RetriesPerState 0`.
  - Smoke evidence:
    `apps/android/build/smoke-regression/20260520-141446/report.md`.
  - Closed this lane after Genres Index shipped.
  - Split Tags Index to `docs/workstreams/android-tags-index/`; People Index
    remains deferred until a richer People IA is accepted.
