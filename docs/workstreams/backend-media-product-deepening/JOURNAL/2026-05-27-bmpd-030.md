# 2026-05-27 - BMPD-030

Added controlled local invitation onboarding.

Changes:

- Added `UserInvitationId`, `UserInvitationRecord`, and invitation status to
  the identity domain.
- Extended identity repositories with invitation create/list/revoke,
  token-hash lookup, and transactional redemption.
- Added `user_invitations` to SQLite/PostgreSQL baselines.
- Added Admin API DTOs for invitation create/list/revoke and Public Client DTO
  for invitation redemption.
- Added Admin routes under `/admin/v1/access/invitations` and Public route
  `/auth/invitations/redeem`.
- Added OpenAPI and SDK coverage for public invitation redemption.
- Added DB and HTTP tests covering one-time redemption, token redaction,
  revocation, roles, and session issuance.

Validation:

- `cargo nextest run -p nako-db invitation_lifecycle --no-fail-fast`
- `cargo nextest run -p nako-server invitation --no-fail-fast`
- `cargo nextest run -p nako-db identity --no-fail-fast`
- `cargo nextest run -p nako-server -E 'test(admin_access) | test(local_session) | test(register) | test(invitation)' --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check` passed with Git line-ending warnings only.

Next task: BMPD-040 Playback Session runtime.
