# 2026-05-27 - BMPD-020

Flattened the migration baselines so fresh SQLite/PostgreSQL databases start
from the current schema instead of replaying old migration history.

Changes:

- Folded SQLite library profile, item metadata, job lease/input, addon
  registration dispatch, addon side-effect apply outcome/report, managed
  artwork cleanup, and addon event delivery columns into their original table
  definitions.
- Removed obsolete SQLite duplicate/drop index and fresh-database de-duplication
  fragments.
- Folded PostgreSQL addon registration dispatch and addon event delivery lease
  and forced replay columns into direct table definitions.
- Removed old `-- From` migration markers from both baselines.
- Added SQLite/PostgreSQL regression tests that reject historical replay
  fragments in the single baseline migration.

Validation:

- `python -m json.tool docs/workstreams/backend-media-product-deepening/WORKSTREAM.json`
- `cargo fmt --all -- --check`
- `git diff --check` passed with Git line-ending warnings only.
- `cargo nextest run -p nako-db --no-fail-fast`
- `cargo nextest run -p nako-server -E 'test(admin_access) | test(local_session) | test(playback)' --no-fail-fast`

Next task: BMPD-030 invitation registration.
