# ECAB-050 Journal

Date: 2026-05-27

## Work Completed

- Selected Chromecast as the first real external casting protocol.
- Recorded the decision in `PROTOCOL_SELECTION.md`.
- Accepted ADR 0042 and added ADR 0043.
- Chose `nako-official-addons` as the protocol sidecar implementation
  repository.
- Chose `oxicast` as the preferred first Chromecast dependency, with
  `cast-sender` as the fallback.
- Deferred DLNA until a renderer device-profile model exists.

## Validation

```powershell
git diff --check -- docs/workstreams/external-casting-adapter-boundary docs/adr
```

Result: passed. Git printed CRLF conversion warnings only.

## Next

Run `ECAB-060`:

- add a first-class renderer-adapter addon resource/scope in `nako`;
- open an official Chromecast renderer adapter sidecar workstream in
  `nako-official-addons`;
- implement a sidecar slice with manifest validation, readiness, payload
  validation, redaction, and Chromecast command-plan mapping.
