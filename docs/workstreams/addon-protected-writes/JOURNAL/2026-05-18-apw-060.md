# 2026-05-18 APW-060

Task: close or split the Addon Protected Writes lane after the first concrete
protected-write apply slice.

## Review Findings

- Workstream compliance: APW-030 satisfies the lane closeout condition by
  proving an accepted Addon Side Effect can become a Taru-owned Canonical
  Metadata write with explicit apply outcome, idempotent replay, audit, and
  redacted responses.
- Code quality: closeout review found one important provenance issue. A scalar
  Addon metadata patch could call full catalog hydration with Addon source and
  rewrite existing provider/NFO label graph sources. This was fixed before
  closeout.
- Missing gates: none after fresh focused and cross-crate checks.
- Residual risk: item metadata persistence plus catalog/search refresh remains
  a two-step workflow, matching existing metadata/NFO behavior. A single unit
  of work remains future design scope.

## Fixes During Closeout

- Added `CatalogRepository::upsert_search_projection`.
- Added catalog helpers for search-only refresh and selected genre/tag label
  hydration.
- Updated Addon metadata apply so scalar patches refresh search without
  rewriting graph label provenance.
- Updated Addon metadata apply so genre/tag patches only replace the touched
  label sets with `MetadataSource::Addon(addon_id)`.
- Added HTTP regression coverage for scalar-only and tags-only patches
  preserving unrelated catalog graph source attribution.

## Split Decision

APW closes with Canonical Metadata protected writes proven. Remaining breadth is
split:

- `docs/workstreams/addon-managed-artwork-artifacts/` owns `artwork_write`,
  Artwork Candidate, Managed Artwork, Taru-Managed Artifact intake, fetch/cache
  policy, resource budgets, and redacted diagnostics.
- `docs/workstreams/addon-library-file-write-policy/` owns subtitle, NFO, and
  sidecar-asset Library File Write policy, target/path derivation, NFO Round
  Trip, backup retention, VFS write reports, and response redaction.

## Follow-Up

Prefer AMAA-010 next if the product priority is artwork import. Prefer
ALFW-010 next if the product priority is subtitles, NFO export, or sidecar
asset writes.
