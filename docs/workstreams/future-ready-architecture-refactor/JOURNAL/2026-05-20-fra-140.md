# FRA-140 — Closeout

Date: 2026-05-20

## Closeout Decision

M61 is complete. The lane shipped the intended architecture baseline instead
of splitting core work into another active lane:

- PostgreSQL-ready persistence boundary with `TaruDatabase` facade,
  SQLite-owned implementation modules, backend-neutral job lease contract
  tests, and optional PostgreSQL proof coverage.
- Runtime composition extraction from `TaruApp`.
- Local Inference separated from source discovery.
- Provider-neutral Metadata Candidate Graph proof slices for provider/NFO
  inputs.
- Semantic Search projection records and SQLite-owned projection persistence.
- Admin/Public API redaction and generated contract hygiene.
- Final deletion sweep for the `taru-api` root compatibility re-export shim.

## Verification

- `cargo check --workspace --tests` passed.
- `cargo nextest run --workspace --no-fail-fast` passed: 466 tests passed,
  4 skipped.
- `cargo fmt --all -- --check` passed.
- `git diff --check` passed with Git CRLF normalization warnings only.

## Residual Scope

The following are future product or architecture lanes, not blockers for M61:

- Production PostgreSQL operations beyond the proof harness.
- Optional SQLite/PostgreSQL implementation crate extraction.
- Richer search backend selection or hybrid indexing.
- Network tunnel provider implementation.
- AI automation runtime and artifact acceptance policy.
- Additional Admin Console read/write surfaces.
