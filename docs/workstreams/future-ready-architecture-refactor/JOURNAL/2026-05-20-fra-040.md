# FRA-040 — SQLite-Owned Adapter Split

Date: 2026-05-20
Status: Complete.

## Intent

Remove the `SqliteStore` god-adapter shape from Taru's public and root
`taru-db` boundaries without losing existing behavior. The split should improve
PostgreSQL portability by making SQLite identity, migrations, connection
runtime policy, row-codec helpers, and repository SQL clearly SQLite-owned
while preserving a small `taru-db` facade surface.

## Changes

- Added `crates/taru-db/src/sqlite.rs` as the SQLite-owned adapter module.
- Added `crates/taru-db/src/facade.rs` with `TaruDatabase` as the public
  database facade.
- Kept `SqliteRuntimeOptions` public for SQLite runtime tuning, but made
  `SqliteStore` crate-private so downstream crates cannot bind to the concrete
  SQLite adapter.
- Moved job persistence implementation to `crates/taru-db/src/sqlite/jobs.rs`.
- Moved SQLite-backed search index implementation to
  `crates/taru-db/src/sqlite/search.rs`.
- Moved SQLite library, library-item, metadata, and scan persistence
  implementations to `crates/taru-db/src/sqlite/`.
- Moved SQLite media, provider-mapping, and catalog persistence
  implementations to `crates/taru-db/src/sqlite/`.
- Rewired scan, metadata, and search transaction helper calls to use
  `crate::sqlite::{media,provider_mapping,catalog}` paths instead of root
  module paths.
- Moved SQLite ingestion, local-inference, staging, playback, and
  user-playback persistence implementations to `crates/taru-db/src/sqlite/`.
- Rewired scan workflow commits to use
  `crate::sqlite::{ingestion,local_inference}` transaction helpers instead of
  root module paths.
- Moved SQLite Addon, automation, event outbox, webhook, catalog-governance,
  source-duplicate, VFS cache, and artwork persistence implementations to
  `crates/taru-db/src/sqlite/`.
- After this slice, root `crates/taru-db/src` no longer contained repository
  implementation modules; it contained facade, codec, contract tests, the
  SQLite module declaration, crate root, and SQLite-specific tests.
- Moved the SQLite row-codec/helper module to
  `crates/taru-db/src/sqlite/codec.rs`; root `crates/taru-db/src` now contains
  only facade, contract tests, the SQLite module declaration, crate root, and
  SQLite-specific tests.
- Moved SQLite runtime connection policy to
  `crates/taru-db/src/sqlite/runtime.rs`.
- Moved SQLite migrations to `crates/taru-db/src/sqlite/migrations.rs` and
  adjusted migration include paths after the module move.
- Updated server app services, HTTP/app tests, and touched downstream crate
  tests to use `TaruDatabase`.
- Moved the FRA-030 job lease contract tests to construct `TaruDatabase`, so
  the contract now proves behavior at the facade boundary instead of the
  SQLite adapter boundary.
- Deleted the broad `sqlite::prelude` shim. SQLite modules now import
  `SqliteStore`, SQLite codec helpers, `taru_core`, `taru_search`, and `sqlx`
  types explicitly instead of inheriting a single large compatibility import.
- Removed the root `taru-db` internal prelude re-export. The public root now
  exposes only `TaruDatabase` and `SqliteRuntimeOptions`.
- Moved main `taru-db` behavior tests from direct `SqliteStore` construction
  to `TaruDatabase` construction. Tests that intentionally verify SQLite
  internals remain inside SQLite-owned modules.
- Confirmed `crates/taru-db/src` now contains only `contract_tests.rs`,
  `facade.rs`, `lib.rs`, `sqlite.rs`, and `tests.rs`.

## Validation

- `cargo check -p taru-core --tests` — passed.
- `cargo check -p taru-db --tests` — passed.
- `cargo check -p taru-server --tests` — passed.
- `cargo check -p taru-automation --tests` — passed.
- `cargo check -p taru-catalog --tests` — passed.
- `cargo check -p taru-events --tests` — passed.
- `cargo check -p taru-library --tests` — passed.
- `cargo check -p taru-metadata --tests` — passed.
- `cargo check -p taru-nfo --tests` — passed.
- `cargo check --workspace --tests` — passed.
- `cargo nextest run -p taru-db sqlite_job_lease_contract --no-fail-fast` —
  passed, 4 tests passed and 57 skipped.
- `cargo nextest run -p taru-db --no-fail-fast` — passed, 61 tests passed.
- `cargo nextest run -p taru-server --no-fail-fast` — passed, 173 tests
  passed.
- `cargo fmt --all -- --check` — passed after applying `cargo fmt --all`.
- `git diff --check` — passed with Git CRLF normalization warnings only.
- After moving search into `sqlite/search.rs`, refreshed `cargo check -p
  taru-db --tests`, `cargo nextest run -p taru-db --no-fail-fast`, `cargo
  check -p taru-server --tests`, `cargo fmt --all -- --check`, and `git diff
  --check` still passed.
- After moving library, library item, metadata, and scan into `sqlite/`,
  refreshed `cargo check -p taru-db --tests`, `cargo nextest run -p taru-db
  --no-fail-fast`, `cargo check -p taru-server --tests`, `cargo check
  --workspace --tests`, `cargo fmt --all -- --check`, and `git diff --check`
  still passed.
- After moving media, provider mapping, and catalog into `sqlite/`, refreshed
  `cargo check -p taru-db --tests`, `cargo nextest run -p taru-db
  --no-fail-fast`, `cargo check -p taru-server --tests`, `cargo check
  --workspace --tests`, `cargo fmt --all -- --check`, and `git diff --check`
  still passed.
- After moving ingestion, local inference, staging, playback, and user playback
  into `sqlite/`, refreshed `cargo check -p taru-db --tests`, `cargo nextest
  run -p taru-db --no-fail-fast`, `cargo check -p taru-server --tests`,
  `cargo check --workspace --tests`, `cargo fmt --all -- --check`, and `git
  diff --check` still passed.
- After moving Addon, automation, event outbox, webhook, catalog governance,
  source duplicate, VFS cache, and artwork into `sqlite/`, refreshed `cargo
  check -p taru-db --tests`, `cargo nextest run -p taru-db --no-fail-fast`,
  `cargo check -p taru-server --tests`, `cargo check --workspace --tests`,
  `cargo fmt --all -- --check`, and `git diff --check` still passed.
- After moving codec helpers into `sqlite/codec.rs`, refreshed `cargo check -p
  taru-db --tests`, `cargo nextest run -p taru-db --no-fail-fast`, `cargo
  check -p taru-server --tests`, `cargo check --workspace --tests`, `cargo fmt
  --all -- --check`, and `git diff --check` still passed.
- After deleting `sqlite::prelude` and moving main behavior tests to
  `TaruDatabase`, refreshed `cargo check -p taru-db --tests` passed.
- Fresh closeout verification:
  - `cargo nextest run -p taru-db --no-fail-fast` — passed, 61 tests passed.
  - `cargo check -p taru-server --tests` — passed.
  - `cargo check --workspace --tests` — passed.
  - `cargo nextest run -p taru-server --no-fail-fast` — passed, 173 tests
    passed.
  - `cargo fmt --all -- --check` — passed after applying `cargo fmt --all`.
  - `git diff --check` — passed with Git CRLF normalization warnings only.
- `rg "SqliteStore" crates/taru-server/src crates/taru-automation/src
  crates/taru-catalog/src crates/taru-events/src crates/taru-library/src
  crates/taru-metadata/src crates/taru-nfo/src -g '*.rs'` — no matches,
  proving no touched downstream crate still refers to the SQLite adapter.

## Concerns

- `TaruDatabase` is currently a simple single-backend facade. It intentionally
  avoids a fake PostgreSQL abstraction until FRA-050/FRA-060 identifies the
  real migration, SQL dialect, transaction, and contract-test proof shape.
- Some SQLite-owned submodule tests still construct `SqliteStore` directly.
  That is intentional: they test SQLite migration/runtime/rollback details
  rather than the backend-neutral facade contract.
- Full workspace nextest was not run for this slice. Full `taru-db` nextest,
  full `taru-server` nextest, workspace test compilation, formatting, and diff
  checks cover the changed persistence facade and server construction behavior
  while keeping the closeout gate proportional.

## Next Slice

Continue with FRA-050. Inventory the SQLite-specific migration, schema,
row-codec, SQL dialect, and fixture assumptions now isolated under `sqlite/`;
then choose the smallest PostgreSQL readiness proof for FRA-060.
