# SVRS-040 Session Journal

Date: 2026-05-29
Task: SVRS-040 Storage Failure Classification And Backoff

## Summary

- Added `StorageFailureClass` to `nako-core` with safe messages and explicit
  retryability.
- Classified WebDAV short range bodies as partial reads.
- Changed VFS cache, library scan/probe, and staging failure persistence to use
  redaction-safe storage messages.
- Added process-local, library-scoped storage backoff in `nako-server` for
  retryable read/probe/stage failures.
- Kept storage apply, cleanup, restore, and write paths on the real backend so
  managed-import compensation is not hidden by a previous read-side backoff.

## Validation

- `cargo nextest run -p nako-core storage_errors_expose_safe_failure_classification --no-fail-fast`
  passed: 1 test.
- `cargo nextest run -p nako-vfs --no-fail-fast` passed: 48 tests.
- `cargo nextest run -p nako-library webdav_scan_records_partial_directory_failures index_service_records_scan_failures_without_blocking_good_sources --no-fail-fast`
  passed: 2 tests.
- `cargo nextest run -p nako-server storage --no-fail-fast` passed: 18 tests.
- `cargo nextest run -p nako-server manifest_recording_backend_rolls_back_reservation_when_stage_fails --no-fail-fast`
  passed: 1 test.
- `cargo check -p nako-core -p nako-db -p nako-vfs -p nako-library -p nako-server --tests`
  passed.
- `cargo fmt --all -- --check` passed.
- `git diff --check` passed with only Windows line-ending warnings.
- `python -m json.tool docs/workstreams/storage-vfs-resilience-and-source-identity/WORKSTREAM.json > $null`
  passed.

## Follow-On

SVRS-050 should expose these signals through Admin diagnostics without leaking
Source Locators, local paths, raw ETags, credentials, or fingerprint values.
