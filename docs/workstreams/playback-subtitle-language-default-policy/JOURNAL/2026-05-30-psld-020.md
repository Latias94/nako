# PSLD-020 Journal - Request-Scoped Subtitle Language Preference

Date: 2026-05-30
Task: PSLD-020

## Work Completed

- Added `preferred_subtitle_languages` to `PlaybackPreferenceContext`.
- Added probe-aware subtitle selection in `PlaybackTargetProfile`: explicit
  requested subtitle stream, preferred language match, then default fallback.
- Reused normalized language preference identity semantics for subtitle
  preferences.
- Kept HTTP/HLS query adaptation empty for subtitle languages until PSLD-030.
- Added playback tests for language match, explicit override, fallback, and
  identity normalization.

## Evidence

- `cargo nextest run -p nako-playback subtitle --no-fail-fast`
- `cargo nextest run -p nako-server playback --no-fail-fast`

## Follow-On

PSLD-030 should make the policy wire-visible through HLS request parsing and
assert generated subtitle rendition default flags from the public playback path.
