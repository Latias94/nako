# PSLD-030 Journal - HLS Subtitle Rendition Defaults

Date: 2026-05-30
Task: PSLD-030

## Work Completed

- Added `preferred_subtitle_language` to the public HLS playlist query path as a
  comma-separated ordered language list.
- Mapped that query into `PlaybackPreferenceContext::preferred_subtitle_languages`
  so HLS uses the same selected subtitle policy as the playback planner.
- Added route coverage for language-selected HLS subtitle rendition defaults,
  explicit `subtitle_stream` override precedence, and normalized request
  identity reuse.
- Exposed the query in OpenAPI, TypeScript SDK, Kotlin SDK, and HTTP API docs.

## Evidence

- RED: `cargo nextest run -p nako-server hls_playlist_route_accepts_preferred_subtitle_language_defaults --no-fail-fast`
- GREEN: `cargo nextest run -p nako-server preferred_subtitle_language --no-fail-fast`
- `cargo nextest run -p nako-server hls --no-fail-fast`
- Focused API contract gate: `cargo nextest run -p nako-api -E 'test(public_openapi_image_contract_uses_public_refs_without_raw_locators) | test(typescript_sdk_includes_auth_version_error_and_pagination_runtime) | test(kotlin_sdk_includes_public_constants_paths_and_wire_types)' --no-fail-fast`

## Follow-On

PSLD-040 should run final closeout gates and decide whether to close the lane or
split follow-ons for persisted settings, UI controls, OCR/burn-in/ASS shaping,
addon readiness, LL-HLS, DASH, DRM, and offline sync.
