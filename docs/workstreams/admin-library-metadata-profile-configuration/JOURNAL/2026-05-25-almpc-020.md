# ALMPC-020 Journal

Date: 2026-05-25

## Summary

Implemented the first Admin Library Metadata Profile configuration slice.

## Changes

- Added Admin API DTOs for profile update/read response and computed scan
  acquisition plan.
- Added `LibraryAppService` methods to read and replace a library's
  `MetadataProfile` through `LibraryRepository::upsert_library`.
- Added `GET` and `PUT`
  `/admin/v1/libraries/{library_id}/metadata-profile`.
- Regenerated Admin Web TypeScript contract.
- Added focused HTTP tests for persisted Admin profile update and next-scan NFO
  skip behavior after disabling scan metadata.
- Added `forced_replay: false` and `replay_reason_code: None` to the ordinary
  Addon Event delivery claim path so the unrelated in-progress forced replay
  fields compile for this test run.

## Evidence

- `cargo run -p nako-api --example emit-admin-typescript-contract -- --output apps/admin-web/src/adminApi/generated/contract.ts`
- `cargo nextest run -p nako-api admin_contract --no-fail-fast`
- `cargo nextest run -p nako-server admin_library_metadata_profile --no-fail-fast`
- `git diff --check`

## Concern

`cargo fmt --all -- --check` still fails on unrelated dirty addon-event
scheduler/replay test files. ALMPC-touched Rust files were formatted with
`rustfmt --edition 2024`.
