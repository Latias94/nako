# WAAI-020 Session Journal

Date: 2026-05-29
Task: WAAI-020

## Summary

Audited and implemented the `web/` Admin Acquisition Intake read-model boundary:

- verified generated Admin symbols for candidate list route/query/response;
- added `AdminApiClient.getAcquisitionIntakeCandidates(query)`;
- added `loadAcquisitionIntake(query)` to the Admin read-model data source;
- added `ADMIN_ACQUISITION_INTAKE_READ_MODEL_FIXTURE`;
- mapped candidates into UI-safe read models using only redacted source fields;
- covered live query serialization, Bearer auth, fixture fallback, and raw-field
  redaction in `src/test/data-source-contracts.test.ts`.

## Notes

WAAI-020 does not add `/admin/acquisition/intake` UI. WAAI-030 should use the
new read-model boundary and keep the route read-only. Watch-folder discovery and
other mutations remain deferred to WAAI-040.

## Validation

- `npm --prefix web run test -- src/test/data-source-contracts.test.ts`
- `npm --prefix web run check`
- `python -m json.tool docs/workstreams/web-admin-acquisition-intake/WORKSTREAM.json`
- `git diff --check -- docs/workstreams/web-admin-acquisition-intake web/src/api/admin web/src/test/data-source-contracts.test.ts`
