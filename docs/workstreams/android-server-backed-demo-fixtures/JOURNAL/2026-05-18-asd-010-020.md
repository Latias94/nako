# ASD-010 / ASD-020 Workstream Opening And Route Discovery

Status: DONE
Date: 2026-05-18

## Scope

Opened `android-server-backed-demo-fixtures` as the durable follow-on for
server-backed Android media smoke fixtures.

Completed:

- `ASD-010`: scope and evidence freeze.
- `ASD-020`: Public Client API route matrix and first fixture provider strategy.

## Decisions

- Android must not fake server-backed Media Libraries, Media Items, Item Detail,
  Media Sources, or playback decisions.
- The first implementation strategy is a real local `nako-server` seeded
  fixture reached by Android through `adb reverse`.
- The fallback strategy is a public-route-compatible local test-server harness,
  but only if real seeded startup is too brittle.
- The first player smoke should prefer direct-play MP4. Android's stale
  `ClientTranscodePlan.inputLocator` requirement was later fixed in `ASD-030`.

## Review

Workstream compliance:

- No blocking findings.
- `DESIGN.md`, `TODO.md`, `MILESTONES.md`, `EVIDENCE_AND_GATES.md`,
  `WORKSTREAM.json`, `HANDOFF.md`, and `ROUTE_MATRIX.md` agree on current task
  `ASD-030`.

Code quality:

- No runtime code changed.
- Discovered Android/Public Client API DTO drift for transcode plans; this was
  fixed in `ASD-030`.

Missing gates:

- No Android or Rust test gate was required for this docs-only slice.
- `git diff --check` is the current validation gate.

## Validation

- `git diff --check` passed on 2026-05-18.

## Next

Run `ASD-030`: create a local seeded `nako-server` fixture startup path and
prove safe fixture responses before wiring Android smoke navigation.
